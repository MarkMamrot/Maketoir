node_modules
.next

# Playwright and gated live-E2E artifacts
/playwright-report/
/test-results/
/.auth/
/.next-e2e/
/.live-e2e-runs/

deploy.zip

# Environment secrets — never commit
.env
.env.local
.env.production

# Credential files — never commit
base64.txt
*.key.json
.npmrc
