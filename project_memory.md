## 2026-09-01 - Curated AI pricing and plan margins

- SuperAdmin AI Usage & Credits now maintains one curated catalogue: Gemini 3.7 Flash, Gemini 3.5 Flash-Lite, Gemini 3.1 Pro Preview, Nano Banana 2, Nano Banana Pro, and Veo 3.1 Standard. The normal screen no longer exposes discovery, SKU mapping, arbitrary model enablement, explicit sell-rate cards, or manual effective-rate creation.
- One editable AUD-per-USD rate converts the reviewed published USD manifest into effective provider costs. Every active plan uses an editable percentage markup; applying pricing updates FX, effective provider rates, the six-model allowlist, and all five plan markups in one transaction while retaining historical rate rows and per-call snapshots.
- Google responses provide usage quantities rather than a monetary request total. Reservation and settlement remain usage based, including long-context Pro, image-output token, and generated-video-second dimensions.
- Runtime model authorization no longer depends on discovered-model metadata. Hourly maintenance continues cycle advancement, account reconciliation, and unknown-call reporting but no longer runs broad model discovery. Dormant discovery tables and endpoints remain for historical audit.
- Built-in Assistant, IMS, Customer Service, Foresight, Product Creative, and Website defaults now use the curated catalogue. Business model preference validation rejects old non-curated IDs. Pricing was not applied to live data because the current FX rate and five approved margins require explicit business input.

## 2026-09-01 - Ask Solvantis commercial model recovery

- Public Sales Assistant and authenticated Ask Solvantis generation had fallen back or failed before provider submission because their `gemini-2.5-flash` default was globally disabled and no longer had complete active commercial rates after canonical model reconciliation.
- Both Assistant orchestrators now default to the curated Flash-Lite model, while retaining their environment overrides. The replacement is globally approved only after the curated pricing set is applied and has complete input/cached-input/output/thinking provider rates.
- A live homepage request returned a genuine persisted answer through the tracked billing gateway. Focused public/private orchestration tests protect the shared default; Help now explains how users should handle a temporary incomplete response.

## 2026-08-31 - POS IndexedDB catalogue persistence

- POS product catalogues and sync watermarks now persist in Dexie-backed IndexedDB under a business-and-location scope. Product refreshes update React memory and IndexedDB atomically, avoiding the `localStorage` size ceiling that made successful manual Sync results disappear after reload.
- Existing tenant-scoped or legacy localStorage catalogues migrate on first read. The old product-cache key is removed only after IndexedDB readback verifies the same product count; carts, parked sales, local sessions, queued sales, and failed sales are not part of this migration.
- POS requests persistent browser storage where supported. IndexedDB failures fall back to the existing localStorage cache without blocking online trading, and catalogue API failures cannot stamp stale data as freshly synced or replace usable products with an empty list.
- Empty-cache session restoration waits for a full catalogue rebuild before opening the scanner. Focused IndexedDB, migration, tenant isolation, quota, and sync-coordinator tests pass.

## 2026-08-31 - Canonical Google model discovery and billing reconciliation

- Google runtime model identity now comes from the paginated Generative Language Models API, not Cloud Billing SKU text. The shared catalog retains display name, version, generation methods, supplied or conservative modalities, context limits, lifecycle, first/last seen times, and raw metadata; models absent from a successful non-empty discovery are retained as retired rather than deleted.
- Cloud Billing reconciliation now uses versioned data mappings from billing-family text to currently discovered canonical model IDs. Seed mappings preserve supported Gemini families, while SuperAdmin can add contains/regex mappings or deactivate mappings with transactional audit records. Unknown model families, unknown metrics/capabilities, conflicts, incomplete pricing, unsupported tiers, and currency issues persist as explainable SKU observations.
- Model availability now requires a non-retired canonical model, global approval, plan priceability, and complete provider pricing for its supported Solvantis capability. Text requires input/output/thinking plus cache when cached content is supported; if any over-200k tariff exists, the complete matching long-context set is required. Image requires input plus image output tokens; video requires per-second pricing. Unsupported embedding, TTS, live, robotics, music, and AQA models remain discoverable but cannot enter normal selectors.
- New provider models default disabled. Discovery never activates rates or enables tenant access. Manual Google rate sync refreshes discovery/reconciliation first, and hourly authenticated AI billing maintenance performs discovery, retirement, SKU observation, cycle advancement, and billing reconciliation.
- `scripts/setup-ai-billing.mjs` created and verified `ai_discovered_models`, `ai_billing_family_mappings`, `ai_billing_mapping_audit`, and `ai_provider_sku_observations` in the shared database. The first live run retained 63 canonical models, marked 10 legacy rate-derived IDs retired, and confirmed Gemini 2.5 Pro and Gemini 3.1 Pro Preview fully priced.

## 2026-08-31 - Dynamic AI plan pricing and provider model governance

## 2026-08-31 - Sage Shopify SKU suffix rollback

- Reverted 3,481 Sage variant SKUs from Solvantis uniqueness-suffixed values to their exact pre-import Shopify export values in both Shopify and Sage IMS. The guarded maintenance matched by live Solvantis variant UUID and revalidated Shopify product, variant, and inventory-item IDs before writing; final readback verified all 3,481 values in both systems.
- Leading apostrophes used as spreadsheet text markers were removed for 17 qualifying suffixed variants. The 244 variants with no populated original export SKU were left on their newer assigned SKU, and unsuffixed variants were not changed.
- Exact-ID before/after evidence is retained in `tmp/sage-shopify-sku-revert-result.csv`. Restoring duplicate original SKUs means legacy SKU-only reconciliation remains ambiguous; stable Shopify IDs remain authoritative.

- Each AI plan now independently uses either explicit active sell rates or a persisted flat markup. Markup mode stores basis points on the plan and derives each customer price from the provider rate active when the AI call starts, rounded up to an AUD micro; it creates no plan-rate rows and leaves all existing explicit rate history unchanged.
- SuperAdmin AI Usage & Credits saves and reloads plan pricing modes and percentages. The active plan-rate table shows only explicit-rate plans, while active provider models can be globally allowed or disabled. Active means the effective window includes the current time, including future `effective_to` values.
- Tenant text, image, video, and general Gemini model endpoints now share the priced allowed-model catalog. IMS, Intel, Customer Service, Product AI Creative, and Setup selectors reconcile stale saved values to that catalog, and the tracked Google billing service blocks direct use of a disabled or unpriced model before provider submission.
- Google Cloud Billing labels Gemini 3.1 Pro Preview standard API SKUs as `gemini 3 pro`. The importer aliases that billing label to runtime model ID `gemini-3.1-pro-preview` and treats terminal `short`/`long` labels as the standard/over-200k context bands. Eight account-specific AUD rates were activated for that model after a conflict-free live preview; `gemini-2.5-pro` and `gemini-3.1-pro-preview` are both allowed with all eight active metrics.
- The shared schema adds `ai_plans.pricing_mode`, `ai_plans.markup_basis_points`, and `ai_provider_models`. Setup and canonical SQL include the changes; an idempotent runtime guard creates/backfills them for deployment because Railway schema access was unavailable locally. Existing currently priced models are allowed during rollout to preserve availability.
- Validation passed 22 focused parser/catalog tests, 469 Vitest files / one skipped, 2,243 tests / one skipped, Help compilation with 57 topics / 465 Assistant chunks, production build, touched-file diagnostics, and diff checks. No plan settings were changed during implementation.

## 2026-08-31 - Gemini Pro and Nano Banana commercial rates

- Google provider-rate synchronization now preserves real preview/image model IDs and supports Gemini 3.1 Pro Preview, Gemini 2.5 Pro, Gemini 3.1 Flash Image (Nano Banana 2), Gemini 3.1 Flash Lite Image, Gemini 3 Pro Image (Nano Banana Pro), and Gemini 2.5 Flash Image (Nano Banana).
- Pro rates use separate `*_over_200k` metrics; runtime charging switches at more than 200,000 prompt tokens including cached input and falls back per metric when Google has no long-context replacement. Nano Banana image output is charged from image output tokens, separated from text output through Gemini usage modality details, with conservative model-specific reservation estimates.
- SuperAdmin Google approval idempotently widens the shared provider/plan metric ENUMs only when required, before opening the effective-rate import transaction. Existing rates and historical usage remain unchanged; plan markup creation automatically includes the newly approved metrics.
- Validation passed 28 focused AI billing tests, 466 Vitest files / one skipped, 2,230 tests / one skipped, Help compilation with 57 topics / 465 Assistant chunks, production build, touched-file diagnostics, and diff checks. Account-specific live SKU approval remains a SuperAdmin action because Google prices are re-fetched at approval time.

## 2026-08-30 - Persistent AI rates and bulk plan markups

- SuperAdmin AI Usage & Credits now shows saved active provider rates whenever the page opens; Google synchronization is only needed to check for provider changes. The table is bounded, retains source SKU and effective-date visibility, and no longer depends on transient Google preview state.
- Plan sell rates can be generated atomically from all active provider rates with a separate optional markup percentage for Starter, Core, Scale, Enterprise, and Platform. Blank plans remain unchanged. The server re-reads provider costs, calculates exact AUD micros as provider cost × (1 + markup percentage), rounds up to one micro, closes prior active rates for selected plans, and retains history.
- The markup endpoint is SuperAdmin-only, validates 0%-1,000% with up to two decimal places, reports operational failures through Runtime Issues, and does not accept browser-calculated prices. No live plan rates were created during implementation or validation.
- Validation passed 18 focused rate-card tests, 465 Vitest files / one skipped, 2,220 tests / one skipped, Help compilation with 57 topics / 464 Assistant chunks, production build, touched-file diagnostics, and final diff checks.

## 2026-08-30 - Guarded Google AI provider-rate synchronization

- SuperAdmin AI Usage & Credits can now retrieve account-specific Gemini prices from the Google Cloud Billing Pricing API in AUD, compare supported standard token SKUs with active provider rates, and explicitly approve selected new or changed rates. Approval re-fetches Google server-side, accepts candidate identities rather than browser-supplied prices, skips exact matches, and preserves effective-dated history.
- The importer prefers account contract prices, records Google SKU/price provenance, and refuses non-AUD, tiered, threshold, service-tier, storage, tool, and modality pricing shapes for manual review. Handled Google connection/import failures create sanitized Runtime Issues.
- Deployment requires `GOOGLE_CLOUD_BILLING_ACCOUNT_ID` (legacy `GOOGLE_BILLING_ACCOUNT_ID` also resolves), valid Google service-account credentials, Cloud Billing API access, and billing-account permissions equivalent to Billing Account Viewer. The Pricing API is Preview. Local live retrieval remains unverified because the current account ID is absent and the local JSON credential is malformed/restricted; fixture and route tests cover the documented API contract.
- `scripts/setup-ai-billing.mjs` added nullable `source_sku_id` and `source_price_name` columns successfully and verified all six shared AI billing tables. Validation passed 458 Vitest files / one skipped, 2,193 tests / one skipped, Help compilation with 57 topics / 464 Assistant chunks, production build, and touched-file diagnostics.

## 2026-08-29 - Cross-tenant AI usage, credits, and enforcement

- The main database now owns AI plans, tenant accounts, effective-dated provider/plan rates, physical provider-call usage, and an append-only account ledger. Prepaid and cycle account-limit funding use exact AUD micros with atomic reservation and settlement; submitted calls with uncertain outcomes remain held until a SuperAdmin explicitly releases them.
- All application Google text, document, image, video, retry, Foresight, Customer Service, private Assistant, public Sales Assistant, and direct REST generation paths now pass through one tracked gateway. A source test blocks future raw Google generation outside that gateway; public prospect usage is assigned to the internal Solvantis Platform account.
- IMS Settings includes read-only Account & AI Credits visibility. SuperAdmin includes cross-business usage/cost/margin reporting, account configuration, credit commands, cycle reset, reviewed unknown release, CSV export, and append-only effective rate creation. Existing and newly onboarded businesses start in observe mode; no commercial rates, balances, or enforced limits were guessed.
- `scripts/setup-ai-billing.mjs` was applied twice successfully: six tables were created on the first run and verified as already present on the second. Validation passed 456 Vitest files / one skipped, 2,186 tests / one skipped, Help compilation with 56 topics / 457 Assistant chunks, the production build, and final diff checks. Authenticated browser validation remains pending because the shared browser had no signed-in session.

## 2026-08-29 - Per-function AI model settings

- IMS Settings now includes an AI Models section populated from the live Gemini text-model list. Businesses can choose separate models for document extraction, catalogue matching, business intelligence/content, and customer service fallback work.
- Supplier invoice and customer-order extraction default to Gemini 2.5 Pro; catalogue matching defaults to Gemini 2.5 Flash. Existing dedicated Website Content, Product Creative, and Customer Service light/capable selectors remain authoritative in their workflows.
- The shared `connections` schema now stores four nullable per-function model choices. The idempotent setup migration was applied twice: four columns were added, three existing connection rows received the recommended document defaults, and the second run changed zero rows.
- Canonical AI Model Settings Help and contextual mapping were added. Validation passed 452 Vitest files / one skipped, 2,176 tests / one skipped, Help compilation with 55 topics / 449 Assistant chunks, the production build, and touched-file diagnostics. Two unrelated existing IMS page `OrderStatus` diagnostics remain.

## 2026-08-29 - Shopify capability outage order recovery

- A missing main-schema online-channel migration made Shopify appear disabled and blocked webhook processing. After channel restoration, an all-day comparison found one absent paid Monsterthreads order and two absent Sage orders; the Sage audit then exposed nine additional Shopify orders present only as unconfirmed Drafts.
- Monsterthreads order #47835 was replayed through its signed `orders/paid` webhook and verified once as Confirmed/Paid with three units. Sage orders #10642-#10652 were replayed as `orders/create`, preserving their Authorized financial state; all 11 are Confirmed, 12 units are committed, each line has one `so_confirmed` movement, and no Shopify order ID is duplicated.
- Sage confirmation initially failed because `ims_stock_movements.channel` was absent even though runtime writes it. The canonical IMS schema and all-tenant catch-up now add nullable `VARCHAR(20)` channel after `movement_type`; the migration added it only to Sage and Solvantis and verified all four tenant schemas.
- Shopify order recovery scripts now renew and persist expiring Dev Dashboard client-credential tokens. Recovery detects both absent Shopify IDs and existing interrupted Drafts, and chooses `orders/paid` only for paid orders while replaying other financial states as `orders/create`.
- The webhook existing-order path now resumes a Draft through normal confirmation, preserving idempotency for already Confirmed/Fulfilled/Cancelled orders. Validation included signed live replay, zero remaining same-day gaps/Drafts, per-order stock commitment and movement readback, 18 focused tests, and a production build.

## 2026-08-29 - Unified Help, Intel & Automation, and single global appearance

- The Help drawer now lists every topic allowed to the signed-in audience and enabled capabilities in one browsable system, grouped into IMS, Intel & Automation, POS, Wholesale, Setup, and Reference. Opening Help selects the exact contextual topic and expands only its major section; search, citations, related topics, and direct browsing can open another section.
- The user-facing Foresight product name is now **Intel & Automation** across workspace navigation, planning and recommendation copy, Admin access, feature rollout labels, IMS links, model-setting guidance, and canonical Help. Compatibility-sensitive `/api/foresight` routes, `src/lib/foresight` names, `foresight_*` tables, feature keys, login destinations, fact/version IDs, and stable Help topic IDs remain unchanged.
- The global Dark/Default skin system and Appearance settings were removed. The former light Default tokens are now the sole global Solvantis palette, with no `data-skin` or `solvantis_ui_skin` runtime. POS terminal themes and customer-facing wholesale, online-shop, loyalty, and payment customisation remain independent and unchanged.

## 2026-08-28 - Shopify Dev Dashboard authentication

- Setup Shopify connections now support existing permanent legacy Admin API tokens and new Dev Dashboard client credentials per business. Client secrets and generated tokens are encrypted; 24-hour tokens are cached and renewed five minutes before expiry through one shared credential resolver used by active catalogue, order, customer, inventory, gift-card, loyalty, webhook, payout, and website paths.
- `scripts/setup-shopify-connections.mjs` added `shopify_auth_mode`, `shopify_client_id`, `shopify_client_secret`, and `shopify_token_expires_at` to the shared `connections` table. It was applied successfully and an immediate rerun made no changes. Existing rows default to `legacy_token`, so Monsterthreads credentials remain unchanged.
- Setup tests Shopify server-side without exposing credentials in the URL or returning saved client secrets. Canonical Connections Help documents the Dev Dashboard install/save/test/webhook sequence. Validation: 439 Vitest files passed / one skipped, 2,099 tests passed / one skipped, production build and migration idempotency passed.

## 2026-08-26 - Runtime Issue triage, query compatibility, and detail UI

- Reviewed Runtime Issues in small batches using each fingerprint's latest event as primary evidence. The live control plane now has 40 fixed and 21 open fingerprints; issues requiring stock, customer-value, external-link, configuration, or current deployment decisions remain open.
- Product Sync status now has a compact Advanced menu for guarded Shopify delink/manual link repair. Delink clears the product and child variant/inventory IDs without deleting Shopify or changing local catalogue/stock data. Manual link verifies the numeric Shopify product ID remotely, rejects duplicate IMS ownership, and restores only unique exact SKU/barcode variant matches in one tenant transaction.
- Shopify order #47750 / IMS ONL-20260818-497112 was fulfilled idempotently after confirming branch transfer 142 delivered its committed unit from Newtown to Warehouse less than three minutes after the original webhook shortfall. Item 98107 is now 1/1 fulfilled, Warehouse SOH is 10 with committed 0, movement 27578 owns the deduction, and Runtime Issue 368 is fixed. Future Shopify webhooks may temporarily take SOH negative only when the shortage is fully covered by recorded PO incoming or outstanding sent/partial branch-transfer supply; they create a Runtime Issue warning and IMS notification, while unexplained or partly covered shortages remain blocked.
- Gift-card issue 248 was a route assumption rather than staff error: Shopify card 559355134168 was created 27 seconds before POS sale 579516 recorded the matching AUD 40 card, then Solvantis attempted to create the supplied code again and retained an unlinked local record after Shopify returned code-taken. Local card 1174 is now guardedly linked with balances unchanged and the issue fixed. Duplicate-code recovery now links only one enabled, unused candidate whose ending characters, currency, initial value, and balance all match; ambiguous candidates remain operational issues.
- Reworked the SuperAdmin Runtime Issue detail drawer for explicit dark-surface contrast, bounded responsive width, sticky actions, readable metadata/events, and automatic issue reopening remains authoritative when a fixed fingerprint recurs.
- Fixed canonical-schema query failures in onboarding supplier counts, IMS dashboard product/brand insights, Shopify loyalty sync, and Xero reconciliation scanning. Production MySQL rejects prepared `LIMIT ?` on the affected paths, so validated and clamped integer literals are used instead.
- Extended and applied the idempotent all-tenant catch-up to align online-shop and stock-allocation join-key collations with each tenant's owning IMS tables. Exact publication, allocation, shipping, CRM, and Sandbox joins were then executed successfully; no stock, order, accounting, gift-card, or customer-value data was mutated.
- Onboarding issue 457 and dashboard issue 447 remain open until the deployment carrying their code fixes is live and rechecked. A fixed fingerprint must not be closed merely because code is ready: a new production occurrence automatically reopens it.
- Added canonical Runtime Issues Help and rebuilt the generated Help/Assistant indexes. Validation passed the full Vitest suite, production build, touched-file diagnostics, and `git diff --check`; authenticated browser inspection of the redesigned drawer remains pending.

## 2026-08-26 - Per-business feature rollouts and Foresight Marketing launch

- Added a shared main-database `business_feature_flags` control plane and SuperAdmin **Feature Rollouts** matrix. Registered features are default-off when no row exists, switches persist the changing SuperAdmin identity, and new rollout entries are added through the typed `BUSINESS_FEATURES` registry rather than new `businesses` columns.
- Foresight reads current flags through `/api/user/me`. Disabled businesses do not see Marketing Activities or Marketing Settings; stale Marketing hashes cannot mount Marketing views and return to Dashboard after flags load.
- Initial `foresight.marketing` rollout was applied and idempotently verified: Monsterthreads is enabled; Monsterthreads DEV SANDBOX, Sage, and Solvantis Pty Ltd are disabled. The migration aligns `business_feature_flags.business_id` to the legacy `businesses.business_id` collation before joining.
- Canonical Feature Rollouts and Foresight Help were updated and generated indexes rebuilt. Validation included focused feature-registry tests, full Vitest, production build, TypeScript diagnostics, migration rerun, live rollout-matrix readback, and `git diff --check`.

## 2026-08-23 - Native shop customer value, refunds, and custom domains

- Signed-in native checkout now reserves one eligible fixed loyalty reward before store credit, freezes value when payment starts, finalizes both ledgers idempotently, and completes fully covered orders without creating a zero-value Stripe PaymentIntent. Native orders persist loyalty as a merchandise discount and Stripe/store-credit settlement as separate sales-order payment rows; loyalty earning uses the shared policy on eligible merchandise after the reward discount.
- Linked IMS customer credit notes own native return stock and accounting. Partial mixed-payment refunds restore the original store-credit portion before an idempotent Stripe Connect refund; provider failure leaves the note incomplete and retryable. Earned and redeemed points reverse proportionally to cumulative returned eligible merchandise.
- `ims_online_shop_refunds` was applied through the all-tenant catch-up to Monsterthreads, Sage, Solvantis, and Monsterthreads Sandbox. The online-shop main schema now has eight tables, 94 columns, and 22 indexes after adding `online_shop_domains`.
- Custom domains require the displayed ownership TXT record, a CNAME to `ONLINE_SHOP_CNAME_TARGET` (or the configured application host), hosting-side domain/certificate setup, and a successful HTTPS probe before activation. Verified hosts are resolved through the control plane and rewritten by middleware into the existing slug storefront; unresolved custom hosts receive a neutral 404 and the hosted `/shop/` address remains available.
- Canonical Online Shop and Customer Orders Help topics, context mapping, `project_overview.md`, and both generated Help/Assistant indexes were updated. Validation: 391 test files passed / one skipped, 1,899 tests passed / one skipped, focused middleware/domain/refund/schema tests passed, all four tenant migrations succeeded, main schema verification passed, and the production build passed.

## 2026-08-23 - Native online-shop foundation and schemas

- Added the independent native online-shop control plane, IMS workspace, layout/page publishing, asset library, product publication, hosted catalogue/product/content routes, and server-revalidated store cart.
- Applied and verified the six main-database online-shop tables. A dry rerun reported no pending tables and verified 76 columns plus 16 indexes.
- Applied the additive online-shop tenant schema to all four registered IMS schemas. The strict rerun reported zero pending columns/indexes and asserted all 11 native tables exist per tenant.
- Legacy Monsterthreads product and variant key definitions were incompatible with three canonical external foreign keys. Fresh canonical schemas retain those constraints; all-tenant catch-up omits only those three constraints and retains native internal constraints and lookup indexes.
- No online sales channel, shop profile, product publication, order, checkout, or payment was activated or created by migration/validation.

# Project Memory & Development Notes

**🤖 AI AGENT INSTRUCTIONS:** 
Always read this file when starting a new session or implementing a feature to understand project constraints, deployment workflows, and important architectural decisions. Proactively update this file as new quirks, dependencies, or workflows are established during development.

---

## 🌐 Deployment Environment
* **Human-friendly visual Help expansion (2026-08-23 - IMPLEMENTED):** The authenticated Help corpus now contains 49 maintained topics and 391 Assistant chunks across IMS, POS, wholesale, Foresight, Setup, and two shared references; the five public prospect sources remain separate and unchanged. All 19 original topic IDs remain stable, while 30 task-focused/reference pages split returns, allocation, PO receiving, stocktakes, POS operations, wholesale buying, Foresight review, integration recovery, roles, glossary, and symptom troubleshooting out of dense overview prose. Task guides use comparison/status/permission tables, checklists, numbered procedures, labelled Note/Tip/Important/Warning callouts, troubleshooting matrices, and worked retail examples. The return guidance now explicitly shows where staff begin, which linked credit note adds stock back once, how customer value is returned, and what must not be repeated; internal phrases such as “sole return-stock owner” and “ledger-owned” were removed. `HelpMarkdown` provides styled callouts and keyboard-focusable horizontally scrolling tables; the drawer searches full section bodies with readable snippets, shows genuine parent/child pages and related topics, and includes shared references for every authenticated audience. Retrieval limits any one topic to two evidence chunks so a long guide cannot crowd out related guidance. The compiler validates task structure, context-section targets, related-topic references, audience compatibility, and unsafe content. Validation: focused Help/Assistant suites passed; full Vitest passed 405 files / one skipped and 1,963 tests / one skipped; production build, diagnostics, and `git diff --check` passed. Authenticated browser validation was unavailable because the shared sessions had expired to login.
* **Bounded agentic Solvantis Assistant research (2026-08-23 - IMPLEMENTED):** The authenticated assistant can now plan and execute up to four sequential allowlisted read-only lookups, carry the accumulated evidence into each decision, stop duplicate tool calls, and force a final Gemini synthesis when the step budget is exhausted. IMS gains bounded recent PO/SO search, low/out/negative stock exceptions, and line-level order summaries; detail rows are tenant-fenced through their verified parent order for legacy rows whose child `business_id` may be blank. POS and wholesale retain their narrower audience/location/account/brand restrictions, arbitrary SQL and write tools remain unavailable, and researched answers disclose human-readable live checks. Prompt `assistant-v3` requires concise operator-facing synthesis that distinguishes live evidence from Help guidance and states limits instead of guessing. Validation: 50 assistant tests and the full 404-file Vitest suite passed (403 passed / one skipped, 1,954 tests passed / one skipped); a real Gemini run selected order search then stock alerts before synthesizing; read-only Sandbox execution verified order search, zero-threshold stock alerts, and bounded order detail; production build, generated Help/Assistant indexes, touched-file diagnostics, and `git diff --check` passed.
* **Screen-aware Solvantis Assistant and Shopify fallback guidance (2026-08-23 - IMPLEMENTED):** The authenticated IMS/POS/wholesale assistant now receives a server-sanitized `visibleScreen` snapshot with a safe semantic outline of the foreground workspace (visible headings, labels, active tabs, and table headings); it never sends raw HTML or form values. Screens can opt into bounded structured operational detail through `data-assistant-screen-context`. Online Sales supplies filters, totals, expanded order references/statuses, and expanded item lines while omitting customer identity; `Shopify Misc Charge` rows also show the original Shopify line title. Canonical Help now explains that protected non-stock SKU `SHOPIFY-MISC` is used when a Shopify line has no variant ID or its variant ID is not linked to an IMS variant. It preserves the source line/value and is not an added fee or Xero policy charge. Prompt `assistant-v2` uses a warmer operator voice, explains causes against visible evidence, and gives the next useful action. The screen-context route boundary caps depth, arrays, object keys, strings, and total text and drops credential/identity-like keys. Retrieval alias `pos` now correctly means point of sale rather than purchase orders. Validation: production Help built 19 topics / 186 assistant chunks, real Gemini output explained a visible fallback line by its original Shopify title, 44 focused assistant tests passed, full 403-file / 1,948-test Vitest suite passed (one skipped), production build and touched-file diagnostics passed.
* **Solvantis Assistant foundation (2026-08-21 - IMPLEMENTED + MAIN DB MIGRATED):** IMS, POS, and wholesale now share an audience-scoped assistant panel backed by 4 reviewed Markdown documents / 17 private retrieval chunks, concise Gemini JSON orchestration, and one allowlisted read-only live-data tool round. Identity and tenant context are server-derived at separate audience routes; wholesale runs in callback-form tenant context, POS tools remain location-bound, selected wholesale brand access is enforced in SQL, requests are bounded/rate-limited, and workflow candidates are HMAC-signed to business/audience/actor before explicit user confirmation can create a finding. Operational failures create Runtime Issues and linked user cases; qualified workflow blockers create separately triaged findings with fixed positive 2-3 business-day service copy. SuperAdmin now has evidence/case/deadline triage with intentional-design documentation events, while the daily developer digest includes unresolved findings and cases due within one day. `assistant_workflow_*` and `assistant_escalation*` tables were applied twice and self-verified in `readyedu_Solvantis`. Validation: full 379-file Vitest suite passed (378 passed / one skipped, 1,842 tests passed / one skipped), production build passed, and authenticated Sandbox Playwright checks at 1440x900 and 390x844 returned a grounded Gemini answer with an allowed citation and verified topmost, contained panel geometry. Local browser validation required an ephemeral `AUTH_SESSION_SECRET` because `.env` does not define the production secret.
* **Canonical Solvantis Help and Assistant knowledge (2026-08-23 - IMPLEMENTED):** Current user-facing product facts now live only in the audience-scoped `docs/help/` corpus. The normal build validates those documents and compiles 19 maintained topics / 185 sections into separate private Help and Assistant indexes; legacy assistant documents and the former overview reference are retired. IMS, POS, wholesale, Foresight/Dashboard, and Setup mount one responsive Help/Ask drawer with stable context IDs, exact subsection routing, menu-grouped navigation, Markdown rendering, and assistant citations that open their owning Help section. Main operations appear near the top and worked examples at the bottom of every topic. Foresight guidance is unified in the same corpus while Planning remains a dedicated governed topic. Copilot instructions require the owning Help topic and context mapping to change with user-visible behavior. The compiler rejects internal/sensitive patterns and audience filtering occurs before retrieval. Validation: 20 focused Help/assistant tests, full 386-file / 1,874-test Vitest suite (one skipped), and production build passed.
* **POS stock zero floor (2026-08-21 - IMPLEMENTED, CODE ONLY):** Completed POS sales remain allowed when recorded SOH is insufficient, but POS-originated deductions can no longer leave `ims_stock.qty_on_hand` below zero. Under the existing stock-row lock, the repository writes a shortage-only `adjustment` movement first (for example `+1` when SOH 0 and one physical unit is sold), then the full `pos_sale` movement (`-1`), ending at SOH 0. This preserves movement arithmetic and movement-driven Xero COGS while allowing the next genuine receipt to increase SOH normally. The same helper covers checkout, manager quantity edits, and deduction paths during void; manager edit/void stock reads now use `FOR UPDATE`. Automatic corrections retain full warning evidence, continue marking received protected allocations at risk, and create persistent IMS notifications from checkout/edit/void routes. Non-stock items and credit-note-owned return stock remain unchanged. Existing negative rows are not bulk-repaired. No schema migration is required because `adjustment` and `pos_sale` are existing movement enum values. Validation: 14 focused tests, full 329-file/1,627-test Vitest suite (one skipped), production build, clean touched-file diagnostics, and `git diff --check` passed.
* **Cash Banking Xero transfer links (2026-08-21 - IMPLEMENTED + MAIN DB MIGRATED):** Cash Banking no longer builds the invalid `Banking/ViewBankTransfer.aspx?BankTransferID=...` URL. Xero's `BankTransferID` remains the authoritative API/audit identifier, while new transfers also persist the destination-side `ToBankTransactionID` and open the established `Bank/ViewTransaction.aspx?bankTransactionID=...` detail page. An authenticated, tenant-scoped redirect route lazily resolves and caches `ToBankTransactionID` through `GET /BankTransfers/{BankTransferID}` for historical deposits, and reports lookup failures through Runtime Issues. Recent deposit rows always show the bank reference, including an explicit fallback for blank legacy/planned rows. `node scripts/setup-xero-tables.mjs` was applied successfully to `readyedu_Solvantis`; `xero_cash_deposits.xero_bank_transaction_id` was verified as nullable `VARCHAR(100)`. Validation: focused 12 tests, full 328-file/1,623-test Vitest suite (one skipped), clean diagnostics in the new route/executor files, production build, and `git diff --check` passed; unrelated in-progress IMS page work still has three editor diagnostics.
* **Status-aware Sales Documents (2026-08-20 - IMPLEMENTED + ALL IMS DBS MIGRATED):** Sales Orders now print explicit Sales Order, Pro Forma Invoice, or Tax Invoice documents under a shared lifecycle policy: Sales Order at every status, Pro Forma only while active and not fully fulfilled, and Tax Invoice only after full fulfilment. Email follows the same boundary and attaches Pro Forma before fulfilment or Tax Invoice afterward; cancelled orders expose no invoice email. Xero's human-facing `InvoiceNumber` is cached in nullable `ims_sales_orders.xero_invoice_number` during create/update/authorise and existing live-detail reads, while the stable Xero `InvoiceID` remains the integration identity and `SO-...` remains Xero's Reference. PDFs never query Xero; Tax Invoices show the cached Xero number plus Sales Order reference when available and otherwise use the unique SO document reference. Tenant settings support customer-document logo visibility, a static invoice note, banking details, and payment instructions; existing sales terms remain shared. `catchup-schema-all-tenants.mjs` added exactly one column to Monsterthreads, Sage, and Sandbox, then added zero on repeat runs; its verifier now asserts nullable `VARCHAR(100)` in every registered schema. Validation: 5 focused files / 41 tests, full 326-file / 1,603-test Vitest suite (one skipped), and production build passed. Authenticated browser document inspection remains pending because no authenticated browser page was shared.
* **CRM live segments + opportunity pipeline (2026-08-19 - IMPLEMENTED + ALL IMS DBS MIGRATED):** Contacts now includes Segments and Pipeline workspaces alongside Contacts and Tasks. Saved segments are live rules over customer type, CRM tags, tax-inclusive POS/sales-order revenue, recent/inactive commerce activity, POS location, and retail loyalty status; evaluation is tenant-scoped, customer-only, bounded to 500 displayed matches with truncation disclosure, opens CRM profiles, and exports CSV. Pipeline stages are tenant-editable and seed New/Contacted/Qualified/Quoted/Won/Lost only when a tenant has no stages. Opportunities retain forecast-only tax-inclusive AUD value, probability, owner, next-action date, lost reason, and contact linkage; movement to Won can convert a lead in place to Retail or B2B while preserving contact ID and CRM history, with no stock/accounting/Xero effect. New `ims_crm_segments`, `ims_crm_pipeline_stages`, and `ims_crm_opportunities` tables use validated VARCHAR state/category values and have `business_id` collations aligned to each tenant's `ims_contacts`; the catch-up migration was applied twice to Monsterthreads, Sage, and Sandbox. Authenticated Sandbox validation created/evaluated/deleted a live segment, created/moved/removed an opportunity, verified forecast recalculation and six-stage loading, and confirmed desktop/narrow tab and board access with zero document overflow; disposable counts were verified at zero. Validation: 318 test files passed / one skipped, 1,551 tests passed / one skipped, production build, diagnostics, and `git diff --check` passed.
* **CRM task maintenance (2026-08-19 - IMPLEMENTED):** Staff can edit follow-up title, details, due date, priority, and assignee from both the global CRM task queue and the customer profile through one shared responsive editor. Reassignment still resolves the selected user server-side against the current business; Advisors remain read-only. Saves reuse existing workspace/profile refreshes so queue filters, counts, contact summaries, task lists, and timeline entries remain current. Authenticated Sandbox validation created one disposable task, updated every field from the queue, opened its customer profile, updated every field again at mobile width, and verified the dialog stayed inside the layout viewport; the task was removed afterward. Browser testing also caught and fixed duplicate React keys in equal-width task table columns. Validation: 316 test files passed / one skipped, 1,544 tests passed / one skipped, clean isolated production build, touched-file diagnostics, and `git diff --check` passed. Do not overlap `next dev` and `next build`; the initial overlapping build corrupted generated `.next`, while removing only `.next` after stopping dev produced a clean build.
* **CRM work management (2026-08-18 - IMPLEMENTED):** Contacts now has segmented Contacts and Tasks workspaces. The global tenant-scoped queue shows open, overdue, and due-today counts; filters by due bucket, priority, assignee, customer/task search; opens the owning profile; and supports complete/cancel actions with immediate aggregate refresh. Contact lists now expose and filter by CRM tag, open/overdue/no follow-up state, and last manual CRM touch (30/90 days or never). One parallelized workspace read uses five bounded business-scoped queries with no per-contact fan-out; the task queue returns at most 500 rows and explicitly warns when truncated. Its split sticky table synchronizes horizontal header/body movement, supports four-arrow scrolling, and uses a 110px frozen customer column so actions remain reachable in narrow IMS content widths. No schema migration was required. Authenticated Sandbox validation used two disposable tasks to prove overdue/today/unassigned/priority/search filtering, task completion refresh, contact follow-up filters, real overflow/header sync/frozen geometry, action reachability, and 390px document containment; both tasks were removed. Validation: 316 test files passed / one skipped, 1,543 tests passed / one skipped, production build completed, touched-file diagnostics and `git diff --check` passed.
* **Incoming stock allocation and channel availability (2026-08-17 — IMPLEMENTED + ALL IMS DBS MIGRATED):** Normal SO demand can now be protected against specific Confirmed/Partially Received PO lines through a quantity-level, tenant-scoped allocation ledger with request-hash idempotency and optimistic revisions. Staff SO detail shows outstanding, allocated, unsourced, FIFO PO candidates, promise dates, at-risk state, release, and reassignment; PO detail shows incoming, protected, free, and linked SO quantities. Destructive PO/SO edits and cancellation/revert are blocked while active allocations exist; ETA changes preserve customer promises but mark them at risk. PO receipt assigns readiness by priority without auto-fulfilment, customer/supplier backorder splits and merges preserve allocation ownership, POS oversells remain non-blocking but visible, and wholesale orders persist quantity-level `indent_qty`. A receipt that fully readies protected demand creates one success notification per affected SO after commit, grouping multiple allocations and linking directly to SO detail; operation replay cannot notify twice, and notification/telemetry failure cannot turn a committed receipt into an error. SO fulfilment now consumes received protection first in the same stock transaction, uses general SOH for any shipment balance, and automatically releases residual future PO protection when a line is fully shipped. Backorder splits consume the shipped parent's received ownership before transferring only unconsumed allocation/receipt quantities to the child. The fulfilment modal shows protected, ready, and incoming quantities and warns when a final shipment will release incoming protection. `catchup-schema-all-tenants.mjs` was applied twice to `readyedu_MonsterthreadsIMS`, `readyedu_SageIMS`, and `readyedu_MonsterthreadsSandboxIMS`; the second pass added zero columns and all allocation/wholesale verification passed. Latest validation: 38 focused allocation/fulfilment tests, full 307-file/1,505-test Vitest suite (one skipped), production build, and authenticated 390px fulfilment-modal validation with no overflow or page errors.
* **Basic contact CRM foundation (2026-08-18 - IMPLEMENTED + ALL IMS DBS MIGRATED):** Contacts now open a dedicated customer profile with commercial summaries, a unified newest-first activity timeline, notes/interactions, follow-up tasks, and reusable tags. Automatic history is derived from the authoritative POS, sales-order, customer-credit, store-credit, and loyalty ledgers; CRM does not duplicate those records or write store-credit/loyalty balances. Request routes use `getImsSession()`, POS history is tenant-fenced through `ims_locations`, activity date ranges are applied in each source query before bounded limits, and task completion retries preserve the original actor/time. Advisors remain read-only. Canonical and all-tenant schemas include interaction, task, tag, and contact-tag tables with per-tenant collation alignment; catch-up migration was applied twice and verified on Monsterthreads, Sage, and Monsterthreads Sandbox. Authenticated Sandbox browser validation covered source-document navigation/history, desktop and 390px layouts, interaction/task/tag writes, duplicate-tag idempotency, and cleanup with no console errors. Validation: 316 test files passed / one skipped, 1,542 tests passed / one skipped, production build completed, touched-file diagnostics and `git diff --check` passed.
* **Stock Availability Workbench (2026-08-17 — IMPLEMENTED):** Orders now includes a read-only Stock Availability work queue backed by tenant-scoped open SO demand and active allocation aggregates. It preserves overlapping exception lenses for unsourced, ready, incoming, at-risk, overdue, and held demand; supports order/product/customer search plus location and protected-supplier filters; shows SOH, outstanding, protected, received-ready, incoming, unsourced, ETA, and primary exception state; and opens the owning SO for audited allocation actions. Its split sticky table follows IMS scrolling conventions with a frozen SO column and four-arrow navigation. Cross-table tenant joins are explicitly collation-safe for mixed legacy tenant schemas. Validation: 6 focused tests, full 309-file/1,511-test Vitest suite (one skipped), production build, real authenticated tenant endpoint check, and an authenticated populated desktop/390px browser check covering lenses, SO deep link, overflow/header sync/frozen geometry/all arrow keys with no page errors. The validation tenant had zero qualifying live demand, so populated rendering used an intercepted read response and made no tenant writes.
* **Allocation Activity History (2026-08-17 — IMPLEMENTED):** Canonical SO Activity History now interprets the replay-safe `allocationFulfilments` already committed in `ims_so_fulfilment_operations.response_json`. Shipment entries summarize received protected quantity consumed and future protection automatically released, with expandable per-line quantities and affected allocation IDs. No duplicate ledger or schema migration was added; operation replay remains the sole event owner. Validation: 14 focused history/fulfilment tests, full 309-file/1,512-test Vitest suite (one skipped), production build, and authenticated desktop/390px expandable SO Activity History rendering with no page errors or mobile overflow.
* **Stock Availability FIFO quick action (2026-08-17 — IMPLEMENTED):** Non-Advisor staff can allocate the first eligible incoming PO drop directly from an unsourced workbench row. The dedicated modal refetches tenant-scoped candidates at open time, caps quantity at the lesser of current unsourced demand and FIFO free supply, shows PO/ETA/free quantity, accepts an optional customer promise date, and submits through the existing idempotent transaction-locked allocation command. Stale demand/supply conflicts remain authoritative server-side and visible in the modal; successful allocation refreshes the workbench. Validation: 20 focused helper/API/allocation-service tests, full 310-file/1,515-test Vitest suite (one skipped), production build, and authenticated intercepted-write browser validation of FIFO selection, quantity error, exact POST payload, refreshed state, and 390px geometry with no page errors or tenant writes.
* **Protected supply exceptions + management report (2026-08-17 — IMPLEMENTED):** Later/removed protected PO ETAs and completed supplier short receipts now capture remaining incoming protection inside the owning transaction, then emit one post-commit warning per affected SO with quantity, allocation count, and direct SO action. Notification failures report a Runtime Issue without rolling back valid PO work. PO cancellation/revert and confirmed line reductions remain transactionally blocked while active allocations exist, so those exceptions cannot silently commit; the shared notifier already supports `cancelled`/`reduced` reasons for future dedicated transfer/release workflows. Reports now includes Stock Availability with unsourced units + tax-inclusive net SO value, received protected ready units + tax-inclusive net SO value, protected incoming units + tax-exclusive AUD PO cost, and unique overdue/at-risk SO promise counts, backed by a line-level scroll table. Validation: 29 focused tests, full 312-file/1,521-test suite (one skipped), production build, real authenticated empty-tenant endpoint query, and populated intercepted-GET desktop/390px browser validation with synchronized horizontal scrolling, no page errors, and no mutations.
* **Standard IMS table scrolling (2026-08-16 — IMPLEMENTED LOCALLY):** Products, PO, SO, customer/supplier credit notes, Contacts, Locations, Branch Transfers, Stocktakes, and Sales Detail now use explicit horizontal overflow, sticky identity columns, sticky headers, and shared 240px arrow-key navigation. Standard lists preserve normal page vertical scrolling; Sales Detail intentionally scrolls its bounded report container vertically. `useTableArrowScroll` centralizes input-safe Left/Right and Up/Down behavior. The reusable `ImsTable` supports fixed widths and a single frozen column, while complex tables use synchronized split header/body tables. The required implementation and real authenticated Playwright geometry checks are documented in `docs/table-scrolling-conventions.md`.
* **CN/SCN/Stocktake normalization Phases 2-5 (2026-08-12 — COMPLETE + IMS DBS MIGRATED + LIVE P9 CLEAN):** Customer and supplier credit-note completion/correction and stocktake Start/Complete/Revert now use named tenant-scoped, row-locked, optimistic-revision commands backed by the shared request-hash operation ledger. Replays return the stored result with `replayed:true`; completion effects and ledger completion commit together. Credit corrections retain original documents and stock movements, require reasons, use Xero preflight/correction state, and expose safe Activity History. Stocktake Complete applies `counted_qty - locked current SOH`, persists `soh_at_apply`, `applied_delta`, and `unit_cost_at_apply`, and appends one movement. Revert Mistaken Stocktake applies `-applied_delta` to current SOH, retains intervening movements, blocks negative compensation, and fails closed for legacy completed counts missing snapshots. Original/reversing Xero journals use snapshot delta/cost with stable idempotency; unknown original outcomes block correction, while post-commit Xero failures leave local correction successful and retryable. Stocktake UI now has PO/SO selector + Go parity, separate count-start/applied metrics, activity/accounting panels, contextual Help, legacy warnings, and mobile horizontal access. The additive correction columns/enums and verifier were applied twice to `readyedu_MonsterthreadsIMS` and `readyedu_SageIMS`; second run added zero columns and all checks passed. Live P9 stocktake 101 on isolated `MT-TEST` proved apply replay, exact compensation across an intervening adjustment, append-only movement sequence `+2, +3, -2, -3` with SOH `2 → 5 → 3 → 0`, one completed Start/Complete/Revert ledger row, no Xero journal required, and final baseline `{SOH:0,incoming:0,committed:0}`. Immutable run `mt-20260812-p9-stocktake-001` is `clean`. Validation: 288 Vitest files passed / one skipped, 1,393 tests passed / one skipped, clean isolated production build, clean diagnostics, and `git diff --check` passed.
* **Mandatory back-office MFA (2026-08-16 — IMPLEMENTED LOCALLY + MAIN DB MIGRATED):** Every email/password back-office login now requires TOTP enrollment or challenge before an eight-hour signed `marketoir_session` is issued; POS cashier PIN login remains separate. Enrollment provides QR/manual setup and one-time hashed recovery codes. Challenges accept replay-protected TOTP or a consumed recovery code and can issue a revocable, hashed remembered-browser token with an absolute 30-day expiry and rotation on use. Password login and MFA verification use durable main-DB rate limits, server-owned destination authorization, and short-lived single-use pre-auth tokens; logout clears session and browser trust. Password reset atomically changes the password, consumes the reset token, clears TOTP/recovery state, expires pre-auth, and revokes trusted browsers. `AUTH_SESSION_SECRET` must remain configured and stable; rollout invalidates legacy unsigned admin cookies and requires every back-office password user to enroll at next login. The shared `readyedu_Solvantis` schema was already verified with 4 user columns, 4 tables, and 9 indexes through idempotent `scripts/setup-mfa-security.mjs --apply`. Validation: 11 focused auth files / 42 tests, full 303-file/1,471-test Vitest suite (one skipped), clean diagnostics, production build, and `git diff --check` passed.
* **CN/SCN/Stocktake normalization Phase 1 foundation (2026-08-12 — IMPLEMENTED + IMS DBS MIGRATED):** Added a pure lifecycle/action policy for customer credit notes, supplier credit notes, and stocktakes plus canonical browser-safe operation keys. A shared tenant-scoped `ims_inventory_document_operations` ledger now binds stable keys to request hashes, document identity, actor, status transition, safe metadata, and replay response. Customer CN Awaiting, Resume Draft, and Cancel and Supplier CN Cancel are named, row-locked, optimistic-revision, replay-safe commands; Cancelled records are retained while Draft-only Delete remains separate. Customer and supplier list rows now use the PO/SO action selector + Go pattern, shared status badges, explicit View behavior, and customer Cancelled filtering. The additive ledger table and tenant-leading unique/history indexes were applied, rerun idempotently, and information-schema verified in `readyedu_MonsterthreadsIMS` and `readyedu_SageIMS`. Validation: 18 focused tests, full 278-file / 1,341-test Vitest suite (one skipped), production build, clean diagnostics, migration syntax/idempotency, and `git diff --check` passed. Browser verification was unavailable because the shared authenticated tab had expired to `/login`. Completion side effects, guarded reversal, activity history, stocktake exact-delta apply/revert, and Xero correction remain later phases.
* **Unified PO/SO correction Activity History (2026-08-12 — PHASE 4 COMPLETE):** The shared tenant-scoped history loader now normalizes amendments, receive/fulfilment operations, outstanding resolutions, PO mistaken-receipt undo, linked customer/supplier credit notes, and replacement source/child orders into one newest-25 timeline. PO/SO detail APIs expose `activity_history` while retaining `amendment_history` as a temporary compatibility alias. Activity document references carry only safe type/ID/number metadata and open the existing PO, SO, customer-CN, or supplier-CN detail flow; raw ledger JSON remains server-side. Help now gives explicit wrong-receipt versus genuine-return decisions, customer-return/replacement examples, immutable-audit guidance, and Xero queue/recovery warnings. Rejected asynchronous customer/supplier credit-note Xero completion jobs report tenant/document-scoped Runtime Issues while local completion remains successful and visibly queued/retryable; Advisor completion is consistently read-only. Validation: 32 focused activity/route tests, full 272-file / 1,302-test Vitest suite (one skipped), production build, clean diagnostics, and `git diff --check` passed. SELECT-only activity and linked-document unions executed successfully against `readyedu_MonsterthreadsIMS` and empty `readyedu_SageIMS`; no production records were created.
* **Phase 3 deterministic SO fulfilment/backorder retries (2026-08-12 — COMPLETE):** `SalesOrderFulfilmentModal` no longer generates a random operation key per click. A shared browser-safe SHA-256 helper derives a stable key from SO ID, loaded `updated_at`, partial-versus-backorder mode, canonical item IDs/quantities at the services' 4-decimal precision, and the stable `confirm_on_shortfall` negative-stock policy. Manual retries after a lost response therefore reach the existing fulfilment/backorder replay path; mode, revision, or quantity changes produce a new key. The explicit negative-stock confirmation reuses the same key because the override authorizes the same shipment rather than changing its identity. This pass also fixed the Confirm button passing its React click event as `allowNegativeStock`, which could suppress the stock-shortfall prompt; it now invokes `submit()` explicitly. Tests prove deterministic canonical keys, changed-input separation, stored fulfilment replay without stock movement, and existing-child backorder replay without stock/line/child writes. Validation: 18 focused tests, full 271-file / 1,298-test Vitest suite (one skipped), production build, clean diagnostics, and `git diff --check` passed. Phase 3 replacement parity and retry closure are complete.
* **Replay-safe PO/SO replacement Drafts (2026-08-12 — COMPLETE + IMS DBS MIGRATED):** Completed/Cancelled POs and Fulfilled/Cancelled SOs now expose `Create Replacement Draft`. Dedicated tenant-scoped POST routes clone one sanitized linked Draft per source and return that same child on retries. Source-row locks serialize same-source requests; connection advisory locks remain held through commit/rollback while allocating order numbers across concurrent sources; tenant-leading unique indexes enforce one child per source. PO replacements copy supplier, location, commercial lines, tax/currency/terms, freight/discount, useful notes, and planned landed costs while clearing invoice/payment/receipt/Xero/Cin7/history state. SO replacements copy customer, location, commercial lines/pricing/cost, tax/currency/terms, freight/discount, channel, and notes while clearing customer PO/payment/fulfilment/Xero/Shopify/Cin7/refund/history state. New line IDs are always generated and source notes are annotated. The UI explains that replacement does not undo receipts/fulfilments or alter Xero and opens the stable child Draft. `catchup-schema-all-tenants.mjs` added nullable source columns and unique `(business_id, replacement_of_*_id)` indexes to `readyedu_MonsterthreadsIMS` and `readyedu_SageIMS`; information-schema verification passed and no replacement rows were created during rollout. Validation: 13 focused tests, full 271-file / 1,295-test Vitest suite (one skipped), production build, clean diagnostics, and `git diff --check` passed.
* **PO/SO operational Change History (2026-08-11 — COMPLETE):** PO and SO detail GETs now load the newest 25 completed tenant-scoped amendment operations through a shared history loader, normalize JSON/object headers, and return previous/resulting status, actor, line-change count, operation ID, and timestamps. Detail modals render an unframed Change History audit list: status work reads like `Confirmed to Cancelled`; commercial amendments read `Order details edited` with affected-line counts. History is ancillary: query failures report a Runtime Issue and return an empty list rather than blocking order viewing. The exact grouped query was executed read-only and passed in `readyedu_MonsterthreadsIMS` and `readyedu_SageIMS`. No migration was needed because it uses the deployed amendment ledger. Validation: shared loader/route tests included in 54 focused tests, full 263-file / 1,253-test Vitest suite (one skipped), clean diagnostics, and production build passed.
* **Revision-stable status retry keys (2026-08-11 — COMPLETE):** Staff status operation keys are now deterministic from order kind, order ID, target status, and loaded `updated_at`, rather than a fresh UUID on every click. Example: retrying Cancel for PO 42 from the same loaded revision reuses the same ledger key and cannot repeat stock; changing the target or acting from a newer revision produces a different key. Create & Confirm uses the newly fetched PO revision. Commercial edit keys remain per-save UUIDs. Validation: pure key contract and status repository suites passed within the 54 focused / 1,253 full tests above.
* **PO/SO status-operation replay safety (2026-08-11 — COMPLETE):** Staff status requests now carry UUID operation keys and SHA-256 hashes into the existing `ims_order_amendment_operations` ledger. Inside the locked PO/SO transaction, a completed matching operation replays before stale-revision, line, or stock checks, so network retries cannot repeat commitments, releases, receipt reversals, or status writes; key reuse with changed status is rejected by the existing hash contract. New operations append actor/before/after header provenance and complete in the same transaction, including same-status SO no-ops so no successful operation remains stuck in `processing`. Route-level cache and Xero hooks intentionally still run after a transactional replay: those hooks are state/idempotency aware, and this lets a retry recover post-commit Xero work without touching stock twice. Shopify/webhook repository calls remain context-free and unchanged. Validation: 47 focused tests, full 262-file / 1,249-test Vitest suite (one skipped), and production build passed after clearing a stale generated OneDrive `.next` artifact.
* **PO/SO status concurrency + desktop receive ownership (2026-08-11 — COMPLETE):** Staff PO/SO status actions now send the loaded `updated_at` revision through the route and compare it inside the repository's `FOR UPDATE` lock before loading lines or mutating stock/status. A stale cancel, revert, release, confirm, or guarded reversal rolls back with the existing amendment conflict instead of acting on a changed order; Shopify/webhook repository callers remain backward compatible because the revision is optional outside staff routes. Create & Confirm fetches the new PO revision before confirmation. This pass also caught and fixed a desktop Receive regression: the modal had stopped using `planPurchaseOrderReceive()` and followed batch receipt with a now-blocked generic complete/partial status request. Save now calls receive batch with `mark_po_received:false`; Save and Complete calls it once with `mark_po_received:true`, prompts on short lines, and requests a held backorder through `create_backorder_po`. No generic status follow-up can double-process or invent receipt stock. Existing Help already matched this behavior. Validation: 48 focused tests, full 262-file / 1,246-test Vitest suite (one skipped), and production build passed.
* **SO generic-status bypass closure (2026-08-11 — COMPLETE):** Authenticated staff can no longer send generic `status='fulfilled'` to force-ship every outstanding SO line or directly cancel/change a `partially_fulfilled` SO. The generic route returns `409 order_lifecycle_conflict` before repository, stock, cache, or Xero work and directs staff to Fulfil/Continue Fulfilment or Resolve Outstanding. The quantity-aware fulfil endpoint remains the owner of shipment deltas and final completion; Resolve Outstanding remains the owner of partial cancellation/backorder financial handling. Repository-level `partially_fulfilled → fulfilled/cancelled` remains intentionally available to Shopify import/webhook callers that mirror externally completed or cancelled online orders. Existing Help already describes the staff workflow. Validation: 23 focused route/repository tests, full 262-file / 1,242-test Vitest suite (one skipped), and production build passed.
* **PO direct-status bypass closure (2026-08-11 — COMPLETE):** The shared PO lifecycle map no longer permits generic `confirmed → complete`, any generic transition out of `partially_received`, or `complete → confirmed`. This closes direct API paths that could force-receive outstanding stock, erase partial receipt history, or reopen a Completed PO outside the named workflows. Confirmed receiving remains owned by the batch receive transaction; partial POs use Continue Receiving/Resolve Outstanding; Completed POs can only use guarded Reverse Receipt & Cancel. Backorder release/cancel and untouched Confirmed revert/cancel remain allowed. Repository tests prove retired transitions execute zero stock/item/status mutation SQL, and the API returns `409 order_lifecycle_conflict` without Xero side effects. Existing Help already described this model and required no change. Validation: 24 focused tests, full 262-file / 1,240-test Vitest suite (one skipped), and production build passed.
* **PO/SO confirmed-order amendments (2026-08-11 — COMPLETE + IMS DBS MIGRATED):** Draft and physically untouched Confirmed POs/SOs now reconcile lines by stable ID inside one tenant transaction instead of deleting and recreating them. Confirmed edits apply exact aggregate stock deltas under row locks: changing PO quantity 10→8 reduces `qty_incoming` by 2; changing an SO from 5 red to 3 red + 2 blue releases 2 red and commits 2 blue. Non-stock SO lines do not affect commitments. Optimistic `updated_at` revisions reject stale editors, and durable operation-key/request-hash ledgers make retries idempotent while retaining actor and before/after provenance. Newly inserted lines record the actual MySQL-generated PO/SO item ID in `result_line_id`; operation-key reuse with a changed hash rolls back before line or stock mutation. Any received/fulfilled quantity blocks generic line editing; partial orders use Continue/Resolve Outstanding and completed records use their controlled reversal or return/credit workflow. In-app Help now documents these boundaries and Xero's editable-versus-settled/locked correction rules with examples. Canonical and all-tenant schemas include both amendment tables and PO item `discount_pct`; `catchup-schema-all-tenants.mjs` was applied and information-schema verified on `readyedu_MonsterthreadsIMS` and `readyedu_SageIMS`. Validation: focused amendment suites 39 tests, full 262-file / 1,237-test Vitest suite (one skipped), production build, and touched-file diagnostics passed.
* **PO/SO lifecycle consistency foundation (2026-08-11 — PHASE 1 COMPLETE IN CODE):** Added a shared pure lifecycle policy with explicit PO/SO transition maps, typed 409 conflicts, and a common user-facing `Completed` category while retaining internal `complete`/`fulfilled` values and physical `Fully received`/`Fully fulfilled` language. Repository status changes now reject every unlisted transition before stock or status mutation. PO/SO commercial edits can no longer use an Admin local-only override when Xero is settled, terminal, locked, or unverifiable; they return structured correction-workflow conflicts. Active actions no longer expose completed/cancelled deletion, completed editing, partial PO `Mark Complete`, or partial `Revert to Confirmed`; partial orders now expose Continue Receiving/Fulfilment and the existing Resolve Outstanding modal, completed POs expose guarded receipt reversal/cancellation, and fulfilled SOs expose notes plus return/credit. Validation: 261 Vitest files / 1,227 tests passed, one skipped, production build passed, and touched-file diagnostics were clean. Next phase is transaction-safe confirmed-order amendment with exact incoming/committed rebalance and stable line identity.
* **SO post-fulfilment edit protection and Monsterthreads repair (2026-08-11 — COMPLETE IN CODE + LIVE ORDER REPAIRED):** Generic SO editing replaced every line, resetting `qty_fulfilled`/cost and leaving a fulfilled parent with unshipped additions. After any shipment, customer/location/pricing/tax/freight/discount/lines are now locked while customer PO and notes remain editable. Partial fulfilment and backorder splitting return structured SOH shortfalls and permit negative SOH only after an explicit warning/confirmation recommending stocktake or adjustment. New confirm/unconfirm movements persist commitment deltas; stock history prefers those durable values and falls back to immutable backorder provenance for legacy split rows. Live Monsterthreads `SO-2026-0001` was repaired under exact-state guards: parent retains only fulfilled `MT-RCA70sSurf-SM` qty 1 at unit cost 24.6209, totals are $40.90 ex tax + $4.09 GST = $44.99, stock remains SOH 2 with one -1 fulfilment movement, cancelled child `SO-2026-0001-B` is unchanged, and unpaid Xero `INV-1184` remains AUTHORISED with one $40.90 line and $44.99 due. Validation: 260 Vitest files / 1,222 tests, clean touched-file diagnostics, live post-write IMS/stock/movement/child/Xero verification, and no retained repair script.
* **Xero COGS `is_stock_item` schema compatibility (2026-08-10 — COMPLETE + IMS DBS MIGRATED):** COGS Reconciliation failed for Monsterthreads with `Unknown column 'p.is_stock_item'` because runtime non-stock exclusion reached production before the additive IMS product column. `catchup-schema-all-tenants.mjs` was applied successfully to Monsterthreads and Sage, adding only `ims_products.is_stock_item TINYINT(1) NOT NULL DEFAULT 1` in each schema; all other 102 columns and 12 indexes were already current. The COGS calculator now capability-checks the tenant schema and treats products as stock items when the column is absent, keeping reports available during future migration drift while preserving explicit non-stock exclusion after migration. Live Monsterthreads July aggregation returned $55,553.75 costed COGS, 354 zero-cost movements, and $108.99 historical exclusion. Validation: focused compatibility tests, 257 Vitest files / 1,192 tests, production build, clean diagnostics, and live all-tenant column/query verification.
* **Xero Sync History timeout recovery (2026-08-10 — COMPLETE IN CODE):** `/api/xero/sync-log?limit=2000` previously performed live Xero status lookups automatically for every linked invoice/credit note after loading history. Monsterthreads had 596 Xero-linked logs, producing roughly 30 serial Xero API batches and an upstream 502 before the route (which only emits 500 itself) could respond. Automatic Sync History and Shopify Payout history loads are now database-only; clicking Refresh opts into `refreshLive=1`, bounded to the newest 40 invoices and 40 credit notes. Whole-route operational failures now call `reportRuntimeIssue()` as `xero/sync_history_load`. No posting or durable sync-state behavior changed. Validation: focused route tests (automatic load makes zero Xero calls; explicit refresh applies live state), 257 Vitest files / 1,178 tests, production build, clean diagnostics/diff check, and read-only production aggregate timing.
* **Xero workspace information architecture (2026-08-10 — COMPLETE):** Xero configuration now has one authoritative editor. The main Xero workspace is grouped into Overview, Setup (Automation, Accounts & Tracking, Payment Routing), and Activity (Sync History, COGS Reconciliation, Shopify Payouts). Settings → Xero Access owns only the immediate-save Advisor permission, a compact Australian GST reference, and command links; the duplicate Ledger Mapping editor and misleading save button were removed. Canonical deep links use `#xero/overview`, `#xero/setup/*`, and `#xero/activity/*`; legacy `#xero`, `#xero/mapping`, `#xero/sync`, `#xero/cogs`, and `#xero/payouts` remain supported. Advisors remain restricted to Accounts & Tracking, and capitalised freight mapping stays visible but disabled. No accounting API, posting rule, readiness rule, schema, or EOD behavior changed. Validation: 256 Vitest files / 1,176 tests, production build, clean diagnostics/diff, canonical and legacy HTTP shell checks, and fresh local `/ims` compile.
* **PO open/edit performance recovery (2026-08-10 — COMPLETE):** Ordinary PO View/Edit/Receive no longer waits for the unused shortfall-financial summary query; summaries are opt-in via `?include=resolutionFinancials` and retain Runtime Issue reporting/fallback behavior. `ImsPORepo.get()` now loads payments, items, landed costs, and files concurrently after the tenant-scoped header, and its payment aggregate is restricted to the requested indexed `po_id`. PO edits replace all lines with one multi-row insert instead of one database round trip per line, preserving the existing transaction and tax calculations. Validation: focused concurrency/query-count tests, 255 Vitest files / 1,161 tests, production build, clean diagnostics/diff check, and local `/ims` HTTP 200.
* **PO detail/receive collation recovery (2026-08-10 — COMPLETE + IMS DBs MIGRATED):** Supplier shortfall financial summaries broke PO detail loading in Monsterthreads because newly bootstrapped resolution/settlement `business_id` columns inherited `utf8mb4_0900_ai_ci`, while that legacy tenant's PO and supplier-credit tables use `utf8mb4_general_ci`. Both View and Receive depended on the same detail route. `catchup-schema-all-tenants.mjs` now idempotently aligns all four supplier/customer shortfall and settlement columns to each tenant's owning PO/SO table and verifies parity; it was applied successfully to Monsterthreads and Sage. PO detail also treats the financial summary as ancillary: a future summary failure reports a Runtime Issue and returns the core PO/items with a warning so receiving remains available. Exact supplier/customer joins pass in both schemas. Validation: 254 Vitest files / 1,158 tests, production build, migration live verification, and local `/ims` HTTP 200.
* **New-business IMS provisioning hardening (2026-08-09 — COMPLETE IN CODE):** Fresh onboarding DDL is now canonical with all 61 tenant tables and every additive column/index from `catchup-schema-all-tenants.mjs`. This fixes a deterministic `store_credit_transactions` DDL failure where indexes referenced undeclared columns, which could leave partial schemas and omit loyalty tables. New onboarding rejects schema collisions, validates tables/critical columns/indexes/triggers through `information_schema` before activation, and compensates only request-owned resources; public registration also cleans Drive artifacts. Provisioning/cleanup and first-login database failures report to Runtime Issues. Valid empty tenants retain zero/empty responses, while onboarding/dashboard failures now return visible retryable errors. Browser smoke testing caught and fixed an invalid retry-button style that crashed the error state and added a responsive onboarding breakpoint so the form/checklist stack below 900px. CI has a disposable MySQL 8 schema-provision test in addition to static full-schema parity, ownership-cleanup, initial-dashboard, onboarding, and all IMS/POS report empty-contract tests. Validation: 254 Vitest files / 1,157 tests passed, one disposable-MySQL test skipped locally because Docker was unavailable, production build and `git diff --check` passed. CI will execute the skipped test against its isolated MySQL service; production catch-up migration has not been run by this code-only pass.
* **Xero controls documentation and rollout (2026-08-09 — COMPLETE):** In-app Xero Help and README now document the Draft/Authorised/locked edit matrix, Needs attention versus immutable All activity, Recheck versus Retry versus Ignore versus Authorise, Admin override consequences, manual Send to Accounts and scheduled digests, transparent policy presets/history, feature-aware mapping readiness, role boundaries, and concrete mismatch/archived-account/unknown-payment examples. Stale claims that online Draft could not use payment sync and that Xero was entirely unidirectional were corrected: Draft is promoted before payment, and Solvantis performs read-only live Xero reconciliation without allowing Xero to mutate IMS operations. Rollout guidance is schema first, deploy, review policy/readiness, manually Recheck and inspect the initial queue, then opt into recipients/digests; production accounting state-matrix exercises remain an operator task in a Xero test organisation. Validation: 249 Vitest files / 1,133 tests, production build, clean diagnostics/diff, and local HTTP 200 for `/ims` and `/ims#xero/sync`.
* **Xero feature-aware mapping readiness (2026-08-09 — COMPLETE):** Ledger Mapping now computes required versus optional status from the saved document policy across role accounts, active PO/SO payment methods, POS location/payment clearing, known online gateways, and location/channel tracking. Every saved reference is checked against live Xero accounts/tracking; missing required and stale mappings become stable `mapping_missing`/`mapping_stale` Needs Attention issues and auto-resolve after recheck. Account, tracking, gateway, and PO/SO payment-method saves also revalidate the live active Xero value before persistence. Optional unused mappings do not become errors, while optional saved mappings are still flagged if stale. Missing POS clearing explicitly affects only that method's Xero posting and never blocks EOD closure. No migration was needed because mapping issues reuse existing reconciliation tables. Validation: 249 Vitest files / 1,133 tests, production build, clean diagnostics/diff, and local HTTP 200 for `/ims#xero/mapping`.
* **Xero policy governance (2026-08-09 — COMPLETE + MAIN DB MIGRATED):** Xero document-policy changes are now Admin/SuperAdmin-only and persist the policy update plus an immutable actor-aware before/after event in one main-DB transaction. No-op saves create no event. Transparent Bookkeeper review, Balanced automation, and Higher automation presets show exact field diffs before populating ordinary editable settings; preset attribution is accepted only while the submitted policy exactly matches it, with no hidden persistent mode. Policies explicitly affect future transitions only. Draft online invoices plus payment sync are supported with an authorisation warning, while genuinely impossible invoice-disabled/payment-enabled states remain blocked. `node scripts/setup-xero-tables.mjs` was applied twice to `readyedu_Solvantis`; all nine event columns and `(business_id, created_at, id)` were verified, and migration created zero audit events. Validation: 248 Vitest files / 1,125 tests, production build, clean diagnostics/diff, and local HTTP 200 for `/ims#xero/sync`.
* **Xero reconciliation scheduled digests (2026-08-09 — COMPLETE + MAIN DB MIGRATED):** Per-business email settings now support `off`, `daily`, or `weekly` cadence, IANA timezone, local hour, and weekly weekday. An hourly GitHub workflow calls a CRON-secret route; pure local-time scheduling handles DST and missed dispatcher runs, while `last_digest_completed_at` suppresses repeat processing in the cadence period. Digests include at most 200 currently open issues, use current mismatch fingerprints and cadence-stable provider/delivery keys, safely reclaim stale `sending` claims with the same provider idempotency key, mark empty periods complete, and leave accounting issue state unchanged. Failures report Runtime Issues and remain retryable; accepted delivery is committed before audit-event fan-out. `node scripts/setup-xero-tables.mjs` was applied twice to `readyedu_Solvantis`; all five cadence columns and defaults were verified through `information_schema`. Validation: 247 Vitest files / 1,118 tests, production build, clean diagnostics/diff, and local HTTP 200 for `/ims#xero/sync`. Local `.env` has no `CRON_SECRET`, so authenticated live dispatch was not invoked; route authorization and delivery behavior are covered by focused tests.
* **Xero reconciliation Send to Accounts (2026-08-09 — COMPLETE + MAIN DB MIGRATED):** Admin/SuperAdmin can configure up to 20 normalized per-business accounts recipients; Admin/SuperAdmin/Advisor can select one or many open Needs Attention issues and send escaped safe summaries through Resend. Every request uses a caller-stable key, provider idempotency, a tenant-leading durable delivery claim, retryable failed state, actor/recipient/issue audit context, and append-only `emailed` issue events. Accepted provider delivery is committed before event fan-out so an event insert failure reports a Runtime Issue without falsely retrying the email. Advisors now default to read-only Sync History even without Ledger Mapping permission and remain blocked from accounting mutations. `node scripts/setup-xero-tables.mjs` was applied twice to `readyedu_Solvantis`; `recipients_json`, all delivery columns, and unique/status indexes were verified through `information_schema`. Validation: 245 Vitest files / 1,105 tests, production build, diagnostics, `git diff --check`, and `/ims` HTTP 200 passed.
* **Xero controls Phase 1 (2026-08-09 — COMPLETE + MAIN DB MIGRATED):** PO/SO payments and PO receipt transfer journals now use shared `xero_accounting_actions` durable claims with stable operation fingerprints and Xero idempotency keys. Payments preflight the live Xero document type, Authorised state, currency, and AmountDue; network/5xx or missing-ID POST outcomes become `unknown` and cannot be blindly reclaimed, while confirmed validation failures remain retryable `failed`. PO receipt journals now use Xero-required signed `LineAmount` entries and the dedicated `po_received_journal` sync type. SO hard deletion is tenant-scoped and Draft-only at route and repository boundaries, with Advisor writes blocked. `node scripts/setup-xero-tables.mjs` was applied to `readyedu_Solvantis`; all action columns plus tenant-leading unique/status/source indexes were verified through `information_schema`. Validation: 227 Vitest files / 1,014 tests, clean production build, diagnostics, and `git diff --check` passed.
* **Backorder Xero recovery and credit management (2026-08-09 — COMPLETE + MIGRATED):** Actionable customer/supplier shortfall failures and Draft reviews now appear in Xero → Sync History. Retry reuses the original resolution and settlement idempotency keys and performs only unfinished Xero work; unknown outcomes remain blocked for manual Xero verification. New default-off `shortfall_credit_draft_first` policy creates shortfall credits as Draft and exposes Authorise & continue. SO/PO detail now shows shortfall financial summaries and controlled append-only unallocate, refund, and reallocate actions with live Xero balance/contact/currency validation and server-validated payment accounts. Backorder provenance stores immutable source-line JSON snapshots, supplier resolutions persist accounting intent, and the supplier resolution service was reformatted for maintenance. `setup-xero-tables.mjs` and `catchup-schema-all-tenants.mjs` were applied; the policy default and all three new columns were verified in `readyedu_MonsterthreadsIMS` and `readyedu_SageIMS`. Validation: 225 Vitest files / 1,003 tests, production build, clean diagnostics, and `git diff --check` passed.
* **Symmetric Resolve Outstanding workflow (2026-08-09 — COMPLETE + MIGRATED):** Customer SOs ship incrementally with durable delta/replay protection. Partially fulfilled SOs and partially received POs now expose Resolve Outstanding: leave open, cancel remainder, or transfer only the outstanding quantity to a held child with provenance and no duplicate stock movement. Unpaid editable Draft/Authorised Xero documents resize; paid/part-paid value creates an Authorised no-restock credit note. Customer settlement supports refund, unapplied credit, or deferred child-invoice allocation. Supplier settlement mirrors it but requires a supplier credit reference/evidence note. Reservations allocate only after child invoice/bill Authorisation; reserved/allocated children cannot be merged or cancelled casually. Durable resolution/action rows preserve operation hashes, states, Xero PaymentID/AllocationID, and safe errors. `scripts/catchup-schema-all-tenants.mjs` was applied and verified against `readyedu_MonsterthreadsIMS` and `readyedu_SageIMS`, including four new tables/indexes and `ims_credit_notes.source='so_shortfall'`. Validation: 223 Vitest files / 995 tests, production build, migration syntax/live verification, and `git diff --check` passed.
* **Runtime Issues repairs (2026-08-04):** Direct main-DB triage found four active clusters: COGS schedule loading collation failures (Issue 3), two IMS browser crashes (Issues 19/78), and empty Gemini URL-judge responses (Issue 73). Production `xero_cogs_settings.business_id` was migrated from `utf8mb4_0900_ai_ci` to match `businesses.business_id` (`utf8mb4_general_ci`), and the original schedule join now returns Monsterthreads' monthly configuration. The cron query also uses binary business-ID equality as a deployment-safe guard. IMS hash navigation now preserves Next App Router history state instead of replacing it with `null`, content-editable descriptions capture `innerHTML` before React releases the event, and COGS buttons no longer pass click events as frequency overrides. Empty successful Gemini `STOP` responses now fail closed as no verified URL, preventing downstream bulk processing without reporting a false JSON parse failure. Validation: 190 Vitest files / 813 tests, Node 20 production build, migration syntax check, production collation/query verification, and local HTTP 200 on port 3002. A direct full TypeScript run still reports unrelated pre-existing errors outside the changed files.
* **POS staff workflow upgrades (2026-08-04):** Order notes now appear on standard, gift, and emailed receipts plus expanded POS report transactions. POS transfer receiving explicitly focuses the barcode field on entry. POS-created branch transfers default to everyone, optionally require the existing location manager PIN, bind the source to the current POS branch server-side, and atomically enter `sent` while committing source stock. Team/DM chat windows are larger and resizable and support up to three JPG/PNG/WebP/PDF attachments of 10 MB each under tenant `UPLOAD_BASE_PATH`; deploy `scripts/catchup-schema-all-tenants.mjs` before using attachments. Preset POS themes remain complete designs; Custom alone exposes light/dark base plus background, menu, search, charge, and heading colours.
* **Hosting:** Vercel (serverless, auto-deploys on push to `main`)
* **Public URL:** TBC (Vercel project URL)
* **Framework:** Next.js 14 (App Router)
* **Database:** MySQL (external — connection via `MYSQL_HOST` env var)
* **Monsterthreads Xero COGS scheduler repair (2026-08-03):** Monsterthreads is configured monthly; GitHub checks hourly at `:17`, but only completed calendar months are eligible. July 2026 contained two eligible, costed movements totaling `$12.05`; no journal run existed because the calculator's alias-based grouping was rejected by production `ONLY_FULL_GROUP_BY`. The calculator now classifies movements in a subquery before aggregation. Manual preview/report/post operations remain inside `runImsForBusiness`, and blocked/failed/unknown cron outcomes durably hold the schedule, report one actionable Runtime Issue, and are excluded from later hourly checks. Only corrected cost blocks can be explicitly resumed; failed/ambiguous Xero outcomes require reconciliation. The Xero UI distinguishes hourly checks from accounting frequency and prevents analysis frequency filters from silently driving posts. `xero_cogs_settings` hold columns were applied successfully with `node scripts/setup-xero-tables.mjs`; Monsterthreads remained monthly with its `2026-07-01` cursor unchanged. The same pass fixed Runtime Issue 18 by reading image dimensions before React clears the load event and stopped expected planner tool date-range validation from being re-reported by the outer dialogue boundary (Issue 7). Validation: 39 focused COGS tests, focused planner regressions, full 180-file/786-test Vitest suite, production build, clean diagnostics, and `git diff --check` passed.
* **Unified website product content workflow (2026-08-03):** IMS Website Content Generator and Admin Pending Online now share supplier-site, brand-site, general-web, and AU-only search semantics. Enabled supplier/brand domains receive direct `site:` searches; Gemini ranks the single most likely exact product page. Product research is internal by default and explicitly excludes shipping, delivery, returns, promotions, payments, stock notices, and other retailer boilerplate while seeking sourced product dimensions without confusing parcel dimensions. WCG is a one-click generate-and-find-photos flow with optional research details; Pending Online defaults new users to automatic mode while preserving saved mode preferences. Candidate photos are larger and are never automatic replacements or deletions: users explicitly add/select desired images and existing product images remain intact. Validation: focused research-rule tests, full 181-file/788-test Vitest suite, clean diagnostics, production build, and `git diff --check` passed.
* **Foresight creative brief governance (2026-08-03):** Creative Review now requires an append-only Admin decision on the exact latest immutable brief: accept for advisory planning, reject with a reason, or request revision with a reason. Accepted briefs cannot be superseded; drafts must be reviewed before another version is generated, and stale version/hash/creative/thread combinations fail closed. Planner tool manifest v6 adds `list_accepted_creative_briefs`, exposing only exact human-accepted brief facts as non-publishing human-authority evidence; drafts and negative decisions remain unavailable to AI planning. Shared table `foresight_creative_brief_review_events` was applied successfully with `node scripts/setup-foresight-main-schema.mjs --apply`; both tenant-leading indexes were verified live. Validation: 180 Vitest files / 781 tests, production build, clean diagnostics, and `git diff --check` passed.
* **Foresight experiment conclusion review (2026-08-02):** Automated deterministic experiment results now require an append-only human `acknowledged` review before they become available to AI planning. Admins can acknowledge a conclusion for advisory planning context or reject it with a required reason; neither action changes evidence, recalculates statistics, or authorizes campaign/strategy mutation. Recommendation Inbox statuses distinguish review due, acknowledged/concluded, and rejected outcomes. Shared table `foresight_campaign_experiment_result_review_events` was applied successfully with `node scripts/setup-foresight-main-schema.mjs --apply`; its `(business_id, result_id, id)` and `(business_id, thread_id, id)` indexes were verified live. Validation: 164 Vitest files / 710 tests, production build, clean diagnostics, and `git diff --check` passed.
* **Foresight Meta experiment launch packages (2026-08-02):** Accepted Meta experiment designs now discover campaigns live through the tenant-bound Meta API, rank names only as suggestions, and require an Admin to map distinct active/paused campaign IDs. Readiness enforces the supported impressions/purchase/negative-feedback measurement contract and exact account/status identity; the SHA-256 confirmation fingerprint binds the accepted experiment, selected campaigns, live campaign metadata, and blockers. Confirmation reruns live validation before writing the immutable shared control-plane ledger and remains explicitly non-authorizing: it creates, edits, schedules, and launches nothing in Meta. Shared table `foresight_meta_experiment_launch_package_confirmations` was applied successfully with `node scripts/setup-foresight-main-schema.mjs --apply`; tenant-leading unique and thread indexes were verified live. Validation: 166 Vitest files / 719 tests, production build, clean diagnostics, and `git diff --check` passed.
* **Configurable PO/SO Xero policy (2026-08-01):** Xero → Ledger Mapping now stores per-business PO and SO status actions (`none`, `draft`, `authorised`) plus independent payment-sync toggles in shared main-DB `xero_document_policies`. Defaults preserve confirmed→Draft, completed/fulfilled→Authorised, and payment sync enabled. Payment sync creates the document if absent, updates it while Draft, authorises it, then pays only when a payment method has a mapped Xero account. Later No sync is a no-op and never downgrades an existing document. Run `node scripts/setup-xero-tables.mjs` before deploying; the table was applied and verified in the configured `readyedu_Solvantis` database during implementation. Payment deletion remains IMS-only and requires manual Xero correction. Validation: 162 Vitest files / 684 tests and production build passed before the final runtime-visibility test addition; final focused policy suite passed 18 tests.
* **Expanded Xero document policy (2026-08-01):** The same Ledger Mapping policy now controls completed manual customer and supplier credit notes, POS EOD invoice/payment sync, daily online invoice status/immediate payments, and opt-in Shopify payout auto-posting. Defaults preserve existing production behavior and keep payout posting manual. POS credit notes remain EOD-owned; Shopify credit notes remain Authorised for payout settlement. POS invoice-only cash rows are excluded from Cash Banking because no clearing payment exists. Auto-post runs only for a durably planned payout, promotes referenced Draft online invoices, and then uses the existing preflighted idempotent executor; blocked/partial/error outcomes report to Runtime Issues. Run `node scripts/setup-xero-tables.mjs` before deploying the application so all shared policy columns exist. The schema was applied successfully to `readyedu_Solvantis`; validation passed 163 Vitest files / 703 tests, clean diagnostics, `git diff --check`, and the production build.
* **PO supplier invoice date → Xero Bill Date (2026-08-01):** PO create/update persistence already stores `supplier_invoice_date`. Xero ACCPAY bill creation and Draft updates now use that value for `Date`; `DueDate` uses the same date plus payment terms. Both fall back to `order_date` only when no supplier invoice date is saved. Full validation passed 163 Vitest files / 704 tests.
* **Build quirk (Shopify/Got):** `shopify-api-node -> got -> cacheable-request -> keyv` can emit Webpack "Critical dependency" warnings in Vercel builds. Mitigated in `next.config.mjs` by externalizing these packages (`experimental.serverComponentsExternalPackages`) and adding a targeted `webpack.ignoreWarnings` filter for `node_modules/keyv/src/index.js`.
* **Shopify customer pull timeout (2026-07-23):** Pulling large Shopify customer catalogs in a single `/api/ims/shopify/sync-customers` request can hit proxy/serverless timeouts and return an HTML `524` page, which surfaces in the browser as `Unexpected token '<'`. Fix: the route now processes Shopify customers in cursor-based batches (100 per request) and the IMS Shopify UI loops through pages client-side until complete.
* **Unified customer returns/store credit (2026-07-25):** Completed manual IMS customer credit notes now atomically restock and issue customer store credit. POS returns create idempotent `source='pos'` IMS credit notes; store-credit settlement issues through CN completion, while cash/card refund leaves store credit unchanged. POS no longer applies return stock separately. Shopify CNs remain external. POS CNs are IMS operational records and stay in the existing POS/EOD Xero flow to avoid duplicate revenue reversal. Deploy `scripts/catchup-schema-all-tenants.mjs` before the application code because the runtime requires the new CN/POS/store-credit linkage columns.
* **Shopify Payments payout reconciliation (2026-07-26):** Online sales now use one combined Xero invoice per completed business day; the retired split/per-gateway routes and setting have been deleted. Non-Shopify gateways pay their mapped share at batch time; Shopify Payments remains due until an actual paid payout supplies its balance-transaction membership. Paid payout webhooks persist canonical transactions and build durable Xero actions for gross invoice payments, externally settled credit-note refunds, processing fees, fee reversals, disputes, reserves, and signed adjustments. A shared idempotent ingester is used by both webhooks and `/api/xero/shopify-payouts/catchup-cron`; `.github/workflows/shopify-payout-catchup.yml` polls paid payouts every six hours over the previous 14 days (configurable with `SHOPIFY_PAYOUT_CATCHUP_DAYS`, clamped to 1–90). Existing `planned`, `partial`, and `reconciled` payouts are not reset; blocked/interrupted payouts can recover. The plan must balance exactly and preflights live Xero invoice/credit-note balances before posting. Automatic execution is intentionally disabled: admins use Xero → Sync History to Replan, Post Payout, or retry only unfinished idempotent actions. The real payout deposit is still matched manually in Xero as a transfer from Shopify clearing to the bank account. Before deployment, run `node scripts/setup-gateway-mappings.mjs` and then `node scripts/setup-xero-tables.mjs`; both are idempotent, and the latter must be rerun after the Phase 2 action-column additions. Shopify requires payout create/update webhooks plus the `shopify_payments_payouts` or `shopify_payments` scope.
* **Xero sync pathway consolidation (2026-07-26):** POS revenue now has one owner: per-payment-method EOD reconciliation. The conflicting POS branch of `/api/xero/sync/daily-sales` was removed and the route is online-only. Manual, login, and cron online triggers all delegate to `syncOnlineDailySalesDay`, which builds the same gateway clearing/payout metadata. `xero_online_batches` is the atomic `(business_id, batch_date)` posting claim; successful historical sync-log rows are backfilled, and invoice/clearing-payment calls use stable SHA-256 Xero idempotency keys. Unsafe legacy `/api/xero/sync/{po-bill,so-invoice,po-payment,so-payment}` routes were deleted; PO/SO payment replay now uses tenant-authenticated `/api/ims/xero/push`. Xero hook fallbacks query shared `xero_sync_log` through the main DB helper, while IMS entities use tenant repositories/`imsQuery`. Customer/supplier CreditNote creation short-circuits stored IDs and uses stable Xero idempotency keys; replay returns the persisted final state instead of unconditional success. Existing deployments must already have `xero_online_batches` from `node scripts/setup-xero-tables.mjs`.
* **Org-wide average cost migration (2026-07-28 — COMPLETE):** Replaced per-location `ims_stock.avg_cost` with a single org-wide weighted average per variant stored in `ims_product_variants.avg_cost`. `ims_stock.avg_cost` is now a convenience mirror (same value for all location rows of a variant, written atomically by `setOrgAvgCost()`). Changed files: `ImsRepository.ts` (7 changes — new `setOrgAvgCost` helper, both PO receive paths, SO fulfill, SCN return, `_btMove`, stocktake GET), `receive/batch/route.ts` (org-level avg formula), `PosRepository.ts` (3 stock reads JOIN variant table), `XeroSyncService.ts` (stocktake journal drops per-location avg JOIN), `replenish/route.ts` (use v.avg_cost), `page.tsx` (branch transfer selectVariant sync, no API call). Tests updated and 200/200 passing. Backfill script `scripts/backfill-avg-cost-monsterthreads.mjs` created (dry-run default, --apply to commit) — run against Monsterthreads before deploying. Full implementation notes in `/memories/repo/avg-cost-model.md`.
* **POS cash banking (2026-07-27):** POS Sales now has a Banking tab for whole closed/unbanked shop days. PosManager and higher can prepare drafts with one physical count per selected day; only Admin/SuperAdmin can confirm/post. New EOD cash plans invoice expected tender (not counted cash), pay that invoice into the shop Cash clearing account, and post `counted - opening float - expected` separately to Cash Over/Short with no GST. Banking then posts each `deposit count - expected custody` discrepancy to Cash Over/Short before making one Xero BankTransfer from Cash clearing to the selected real bank. Durable source/action rows, unique source reservation, stable Xero idempotency keys, atomic posting claims, and retry-only-unfinished behavior protect the workflow. Existing paid EOD rows without a versioned action remain legacy and are not reclassified. Reports → Cash Banking provides cross-shop audit/CSV; posted corrections are made manually in Xero and recorded as an Admin audit note. Before deploying, run `node scripts/setup-xero-tables.mjs` against the main DB. Existing Xero connections must reconnect once to grant the newly requested `accounting.transactions` scope required for BankTransactions and BankTransfers. Configure Cash Over/Short, each shop's Cash clearing account, and default deposit bank before use. Validation at implementation: 35 Vitest files / 170 tests and production build passed.
* **Foresight recommendation outcomes (2026-07-29):** Approved and rejected paid-media decisions receive one idempotent seven-day follow-up after a complete authoritative observation window. Outcomes classify metric direction separately from whether the original condition resolved; missing commerce or blocked COGS defers measurement for retry. Results are explicitly observational and do not claim that an approved proposal was executed or caused the change. Shared table `foresight_recommendation_outcomes` is installed by `node scripts/setup-foresight-main-schema.mjs --apply` (already applied locally). Post-sync and manual Foresight evaluation both process due outcomes. Validation at implementation: 77 Vitest files / 326 tests, production build, and 390x844 / 1440x900 browser checks passed.
* **Foresight AI-human planning foundation (2026-08-01):** Added strict versioned plan contracts that separate cited facts, human context, assumptions, inferences, and proposals; factual statements must reference known fact IDs, selected options must exist, and all AI-drafted actions are structurally non-executable. Plans have canonical SHA-256 hashes and deterministic Markdown renderings. New tenant-scoped shared tables persist planning threads, messages, bounded tool-call audits, immutable parent-linked plan versions, recommendation/initiative/strategy links, and deterministic validation records. `ForesightPlanningRepository` enforces business scoping and optimistic thread revisions so concurrent AI/human edits cannot silently overwrite each other. Repository-owned strategy-interviewer and initiative-planner prompts are versioned Markdown with reproducible hashes and explicitly reject chat-based authorization. `node scripts/setup-foresight-main-schema.mjs --apply` was applied successfully and all six planning tables/indexes were verified in the configured main DB. No model route or UI is connected yet; the next slice is the read-only typed tool registry and strategy/recommendation dialogue APIs. Validation: 124 Vitest files / 531 tests, clean diagnostics, and production build passed.
* **Foresight plan review and locking (2026-08-01):** Passing structured plan versions can now be submitted from Planning Workspace, which atomically verifies the exact latest version/hash and deterministic validation before locking the thread. Recommendation Inbox renders the selected option, risks, non-executable actions, success metrics, and guardrails, then allows Admins to accept, reject, or request revision with an append-only review event. Rejection/revision requires a note; revision reopens drafting for a new immutable version, while submitted/accepted plans cannot be superseded through the draft API. Plan acceptance is explicitly separate from recommendation approval, live preflight, and platform execution. Shared table `foresight_plan_review_events` was applied with `node scripts/setup-foresight-main-schema.mjs --apply` and its tenant-leading indexes were verified. Validation: 139 Vitest files / 588 tests, production build, clean diagnostics, and `git diff --check` passed.
* **Foresight campaign deliverables (2026-08-01):** Accepted plans can now generate immutable, versioned campaign packages for campaign brief, Meta, Google Ads, and Klaviyo. The versioned `campaign-deliverables-v1` prompt receives only facts already cited by the exact accepted plan; strict parsing rejects unknown claim citations, plan identity mismatches, and any asset with `publishable` other than `false`. Recommendation Inbox provides channel selection, complete draft inspection, review notes, and append-only accept/reject/revision decisions. Accepted packages cannot be silently superseded; rejection/revision requires a note before another child version can be drafted. There is no publish, schedule, send, upload, or advertising mutation control. Shared tables `foresight_deliverable_versions` and `foresight_deliverable_review_events` were applied and their tenant-leading indexes verified. Validation: 143 Vitest files / 598 tests, production build, clean diagnostics, and `git diff --check` passed.
* **Foresight external implementation attestations (2026-07-29):** Every recommendation now has a deterministic non-executable manual dry-run preview. Admins can record one immutable external implementation only after approval, with exact proposal-hash validation, tenant-local implementation date, and required details of what actually changed. The server rebuilds the persisted preview from the locked stored proposal rather than trusting browser action data. Approved outcome windows begin after implementation; approved-but-unimplemented proposals are excluded, while rejected outcomes still begin after rejection. Solvantis performs no Google, Meta, or Klaviyo write. Shared table `foresight_recommendation_implementations` is installed by `node scripts/setup-foresight-main-schema.mjs --apply` (already applied locally). Validation: 78 Vitest files / 334 tests and production build passed.
* **Guarded Google Ads budget increases (2026-07-30):** Approved `profitable_growth_opportunity` recommendations can execute eligible Google campaign budget increases through the same fresh live preflight, exact confirmation fingerprint, idempotent mutation, read-back verification, audit receipt, and rollback flow used for reductions. Increases are hard-capped server-side at 10%, exclude revenue-less evidence, use the same configured ROAS deterioration tolerance as the recommendation rule, and block shared, disabled, mismatched, materially deteriorating, or invalid live budgets. Marketing Settings stores a tenant-scoped Budget Change Alerts recipient in the main `config` table; a valid recipient and `RESEND_API_KEY` are mandatory before preflight can become executable. Verified changes send an awaited Resend warning with an execution-bound idempotency key; delivery failure is reported separately and never rewrites a verified Google execution as failed. Validation: 106 Vitest files / 443 tests and production build passed.
* **Guarded Meta Ads budget reductions (2026-07-30):** Approved `review_budget_reduction` recommendations can execute exact campaign- or ad-set-owned Meta daily-budget reductions after a fresh tenant-bound account/entity preflight. Lifetime budgets, inherited ad-set budgets, inactive/mismatched entities, stale fingerprints, missing alert recipients, and unavailable email service block execution. The executor uses one atomic shared execution claim, submits only positive integer account-minor-unit budgets, verifies by live read-back, records platform-correct immutable audit events/receipts, sends an execution-idempotent Resend warning, and never replays an in-progress mutation. Verified Meta executions have a separate fresh-state rollback that restores the stored original budgets only while live values still match the execution receipt; one compensation attempt is retained and interruption recovery is read-back-only. Meta OAuth now requests `ads_management`; existing read-only connections must reconnect before writes can succeed. Validation: 115 Vitest files / 478 tests, clean diagnostics, and production build passed.
* **Meta campaign visibility (2026-07-30):** Foresight campaign/ad-set watch lists are diagnostic contributors, not a complete active-campaign directory. An entity must have spend in the current strategy evaluation window; zero conversions do not hide it and instead add `spend_without_platform_revenue`. Results are ranked and limited to three campaigns plus three ad sets per entity type. The main marketing sync previously hard-coded retired Meta Graph API `v19.0`, which could prevent insight rows from being ingested even though OAuth/account status worked; it now uses `META_GRAPH_API_VERSION` with the same `v25.0` default as OAuth and automatic monitoring. Run a fresh Meta marketing sync after deployment to populate newly active campaigns.
* **Automatic post-execution monitoring (2026-07-30):** The tenant-safe daily Foresight cron now refreshes Google Ads, Meta Ads, and authoritative IMS commerce observations for businesses with a successful execution still awaiting its seven-day outcome, then evaluates due outcomes through the previous completed local day before generating the operations digest. The narrow monitoring sync records each source independently in the existing sync ledger and skips all credential/platform work for businesses without active monitoring; it has no advertising mutation client. Successful execution receipts expose their MySQL-local completion date, show the exact follow-up window and first assessment date, and remain visible as `monitoring_active` daily work until an outcome exists. Validation: 109 Vitest files / 455 tests and production build passed.
* **Cross-organisation Runtime Issues inbox (2026-08-01):** SuperAdmin → Runtime Issues aggregates operational failures from every organisation in shared main-DB `runtime_issues` plus append-only `runtime_issue_events`. `reportRuntimeIssue()` sanitizes credentials, preserves valid bounded JSON context, fingerprints recurring failures, increments occurrence counts, and reopens fixed issues when they recur. Existing Xero error logs, Shopify error/partial logs, `notifySyncFailure()`, high-risk webhook/cron/POS integration catches, authenticated React root crashes, and Next process-level unhandled failures report centrally. First critical severity and recurrence-after-fix use a durable DB claim for an awaited Resend alert; delivery failures restore the claim without failing the originating operation. Recipient is comma-separated `RUNTIME_ISSUES_ALERT_EMAIL`, falling back to active SuperAdmin emails. `.github/workflows/runtime-issues-digest.yml` sends the unresolved digest daily at 21:15 UTC through the secret-authenticated cron route. Run `node scripts/setup-runtime-issues.mjs` against the main Railway DB before deploying; it is idempotent and was applied to the configured database during implementation. Validation: 121 Vitest files / 513 tests, production build, clean diagnostics, focused alert concurrency/failure tests, and a live DB aggregation probe passed.
* **Public prospect sales assistant (2026-08-23):** Landing and pricing now share an anonymous, public-knowledge-only Gemini sales assistant with consent-scoped lead capture, Resend alerts, attribution, transcript/final-prompt insight, integration demand, SuperAdmin lead/catalogue tools, deletion, DB rate limits, and 12-month de-identification. Shared-main schema was applied idempotently to `readyedu_Solvantis` with 16 integration offerings; no tenant schema is involved. `.github/workflows/prospect-conversations.yml` calls the bearer-authenticated maintenance route hourly. Railway requires a dedicated `SALES_ASSISTANT_HMAC_SECRET` of at least 32 characters (the code can fall back to `AUTH_SESSION_SECRET`, but a dedicated secret is preferred), plus `GEMINI_API_KEY`, `CRON_SECRET`, `APP_URL`, `RESEND_API_KEY`, `RESEND_FROM_EMAIL`, and `SALES_LEAD_RECIPIENTS`; GitHub requires matching `APP_URL` and `CRON_SECRET` secrets. Validation: production build, 402 Vitest files / 1,945 tests, a real anonymous Gemini chat/restore/delete flow, and 1440x900 plus 390x844 Playwright screenshots with no page or panel overflow passed. Browser validation prompts were sanitized afterward.
* **Foresight daily operations digests (2026-07-29):** The recommendation inbox now has a deterministic daily work queue for pending approvals, approved-but-unimplemented recommendations, near-expiry items, blocking evidence issues, and newly measured outcomes. One refreshable `daily_operations` snapshot is stored per business-local date in shared table `foresight_digest_runs`; all reads/writes are main-DB tenant-scoped. Admins can refresh in-app, and `.github/workflows/foresight-daily-digest.yml` invokes the secret-authenticated cron daily while isolating failures per business. No advertising or lifecycle platform credentials/writes are involved. Deploy schema first with `node scripts/setup-foresight-main-schema.mjs --apply` (already applied locally). Validation at implementation: 97 Vitest files / 401 tests and production build passed.
* **Foresight weekly performance digests (2026-07-29):** The recommendation inbox now includes a deterministic `weekly_summary` comparing the seven business-local days through yesterday with the immediately preceding seven days. Paid-media spend, authoritative online revenue, MER, and contribution POAS are reported current-versus-prior; POS revenue remains separate context, platform attribution remains diagnostic only, and Klaviyo remains lifecycle coverage only. The summary also records recommendation decisions, implementations, measured outcomes, data-quality notices, and top diagnostic contributors. Admin refresh and the secret-authenticated multi-tenant cron share the same tenant-scoped service; `.github/workflows/foresight-daily-digest.yml` schedules weekly generation at 21:05 UTC Monday and treats HTTP 207 as failure. Existing `foresight_digest_runs.digest_type VARCHAR(32)` supports the new type without a migration. Validation: 98 Vitest files / 408 tests, clean production build, dashboard HTTP 200, digest auth/cron guards, and real idempotent cron generation for both configured businesses (2 generated / 0 failed) passed.
* **One-click Meta Ads connection (2026-07-29):** Setup → Connections now uses tenant-bound Meta OAuth instead of asking operators to create and paste system-user tokens. The server exchanges the code for a long-lived token, discovers accessible ad accounts, auto-selects a sole account or presents a chooser, and stores the selected account plus encrypted token in the existing `connections` row. OAuth state is HMAC-signed, nonce-bound, ten-minute limited, and tied to the signed-in user/business; pending multi-account tokens stay in a short-lived HttpOnly cookie. The generic connections API no longer returns decrypted Meta tokens, and card saves are field-scoped so saving another integration cannot erase OAuth credentials. Railway requires `META_APP_ID`, `META_APP_SECRET`, and optionally `META_GRAPH_API_VERSION` (defaults to `v25.0`); the Meta app redirect URI is `https://solvantis.com.au/api/meta/callback` with `ads_read`, `ads_management`, and `business_management` access. Existing read-only connections must reconnect before guarded Meta budget execution can write. No schema migration is required. Validation: 101 Vitest files / 416 tests and production build passed.
* **One-click Google Ads + Analytics connection (2026-07-30):** Setup → Connections uses one tenant-bound Google OAuth grant for Google Ads and GA4, discovers accessible Ads customers and Analytics properties, and revalidates selections before saving. The existing encrypted `google_ads_refresh_token` is shared server-side by both services; it is no longer returned by the generic connections API, and no schema migration is required. Railway uses `GOOGLE_ADS_CLIENT_ID`, `GOOGLE_ADS_CLIENT_SECRET`, and `GOOGLE_ADS_DEVELOPER_TOKEN`. Google Cloud must enable Google Ads API, Google Analytics Admin API, and Google Analytics Data API and register `https://solvantis.com.au/api/google/callback`; requested scopes are `adwords` and `analytics.readonly`. Validation: 102 Vitest files / 421 tests and production build passed.
  * The OAuth button, returned account/property selectors, selected IDs, live tests, change-account action, and disconnect action all live in one full-width Google Ads & Analytics card. The former manual GA4/Google Ads cards and `/api/sync/{analytics,google-ads}` connection-test routes were retired.

## � Report date filtering standard
- IMS reports that filter by date should use the shared date-range picker in [src/app/ims/views/reports/reportFilterHelpers.tsx](src/app/ims/views/reports/reportFilterHelpers.tsx) rather than bespoke From/To inputs.
- When adding or updating an IMS report, wire the selected range into the API request using the same `SBDateRange` shape (`kind: 'window' | 'range'`) and keep the UI consistent with Sales by Branch.
- If a report already uses a custom date filter API (`from`/`to`), accept the shared range format in the UI and map it to the API parameters.

## �🚀 Deployment Workflow

### Normal Deployment (Fully Automatic):
1. **Develop locally** and commit/push to `main` via GitHub Desktop.
2. **Vercel** detects the push, builds, and deploys automatically (serverless functions for API routes).
3. No restart needed — each deploy creates a fresh deployment.

### Environment Variables:
- Managed in **Vercel Dashboard → Settings → Environment Variables**.
- Includes: `MYSQL_HOST`, `MYSQL_PORT`, `MYSQL_USER`, `MYSQL_PASSWORD`, `MYSQL_DATABASE`, `ENCRYPTION_KEY`, `XERO_CLIENT_ID`, `XERO_REDIRECT_URI`, and all other secrets.
- `.env` is used locally only and is gitignored.

### Monitoring:
- Check the **Vercel Dashboard** for build status, logs, and function errors.
- Runtime logs available under the Logs tab per deployment.

### Previous Hosting (archived):
- Was on PromptWebHosting (cPanel + Phusion Passenger) with GitHub Actions deploy pipeline.
- Migrated to Vercel for simpler serverless deployment and no memory constraints.

## 🛒 POS System (built this session)
* **DB tables**: 5 tables in the IMS database (`pos_users`, `pos_sales`, `pos_sale_items`, `pos_payments`, `pos_eod_reconciliations`). Created by `scripts/setup-pos-tables.mjs`.
* **`ims_stock_movements`** ALTERed to include `pos_sale` and `pos_return` in `movement_type`/`reference_type` enums.
* **Repository**: `src/lib/db/PosRepository.ts` — `PosUsersRepo`, `PosSalesRepo`, `PosEodRepo`, `PosReportsRepo`.
* **API routes** (13 routes under `src/app/api/pos/`): auth (login/logout/me), users, settings/payment-methods, products, locations, sales, sales/[id], sales/[id]/payments, eod, reports/daily, reports/graph.
* **POS app**: `src/app/pos/page.tsx` — full browser POS with device setup, cashier login, product search/barcode, cart with discounts, payment modal (offline queue support), parked sales, receipt printer, EOD reconciliation (with denomination breakdown), and reports.
* **Setup integration**: `src/app/setup/page.tsx` has a POS tab for managing cashier users and payment methods.
* **IMS integration**: `src/app/ims/page.tsx` has a "POS Sales" view — date + location picker, summary cards (revenue, count, by method), expandable transaction list with items and payments.
* **Auth**: POS uses `pos_session` cookie (16-hr), separate from admin `marketoir_session`. bcryptjs (12 rounds) for cashier passwords.
* **Payment methods**: stored in main DB `config` table via `ConfigRepository.set(bizId, 'POS_PaymentMethods', JSON.stringify(methods))`.
* **Offline queue**: Failed sales are queued in localStorage and retried on reconnect/login.

## 🧠 AI Helper Notes
* AI Helper now supports a running in-session chat with role labels: **Professor KnowItAll** and **The Business**.
* Chat history is sent back to `/api/ai/ask` on each message so responses use current-session context.
* Ending a chat opens a save modal. If saved, summary is written to a Drive spreadsheet named **BusinessChats** in the business folder (`Config!FolderID`).
* Business chat summaries are stored in sheet `Chats` with boolean classification fields: `inventoryManagement`, `marketing`, `businessStrategy`, `websiteManagement`.

## 📦 Inventory Notes
* Inventory Management now includes an **Order Planner** view backed by `/api/inventory/order-planner`.
* The planner reads the synced `Products` and `Suppliers` sheets, calculates reorder suggestions from branch-level sales quantities plus `createdDate`, and can save each plan to a **Draft Orders** spreadsheet in the business Drive folder.
* Pushing a draft to Cin7 currently posts directly to `/PurchaseOrders` and requires a single supplier plus a selected branch.
* Sync Inventory includes a **Branch List** source backed by `/api/sync/branches`, which writes Cin7 branch records to the `Branches` sheet in the resolved inventory system spreadsheet.
* Branch-dependent flows (`/api/inventory/order-planner`, `/api/sync/products`, `/api/sync/sales`) now read active branch metadata from the synced `Branches` sheet first, then fall back to live `/Branches` API if the sheet is missing/empty.

## 🏷️ IMS — Pending Sync Work
* **product_type from Cin7** — Cin7 returns a `Type` field on product records. The Cin7→cache sync currently does NOT write this to the `products` cache table.
  * **TODO**: Add `product_type VARCHAR(255)` column to the `products` cache table (or confirm it already exists via schema).
  * **TODO**: In the Cin7 sync route (`src/app/api/sync/`), map `cin7Product.Type → product_type` when inserting/updating rows.
  * **TODO**: In the IMS import products route (`src/app/api/ims/import/products/route.ts`), include `product_type` in the upsert into `ims_products`.
  * The IMS products view already has a `product_type` column in the table and filter dropdowns — it will populate automatically once the sync is updated.

## 🔗 Xero Integration — Phase 1 Status & Next Steps
* Phase 1 OAuth scaffolding is complete (connect/callback/status/disconnect routes + Connections UI card).
* **✅ DONE (architectural fix):** `xero_client_id` / `xero_redirect_uri` moved back to `.env` — they are app-level config, not per-business. The Xero card in Connections now shows a warning if env vars are missing, and the Connect button when they are set.

### POS location/payment clearing accounts (2026-07-25)
* POS EOD invoice lines always post to the global Sales Revenue mapping with the existing location tracking option. A separate required location × payment-method matrix selects the Xero BANK account that receives the payment.
* Missing matrix cells block only that payment method's Xero posting; EOD/register closure continues and other mapped methods still sync.
* EOD sync persists the AUTHORISED invoice ID before calling Xero `/Payments`. Payment failures retain the invoice and retry only the payment, preventing duplicate invoices.
* Legacy `xero_pos_payment_mappings` rows are revenue-semantic and are intentionally not migrated. Existing EOD invoices have `xero_payment_required = 0` and are never backfilled.
* Deployment order: run `node scripts/setup-xero-tables.mjs` against the main DB, then `node scripts/catchup-schema-all-tenants.mjs` for every IMS tenant before deploying code that writes the new EOD payment-state columns.

## 🛒 POS System — Updates (2026-06-23)

### Tax Handling (IMPORTANT)
* **Product prices are stored tax-inclusive (Australian GST 10%).** Never add tax on top — always extract it.
* EOD Accounting columns: `taxExc = salesAmt / (1 + rate)`, `gst = salesAmt - taxExc`, `taxInc = salesAmt`.
* `effectiveTaxRate` is derived as `tax_total / total_exc_tax` from `pos_sales` day totals (= 0.10 for standard GST). Fallback: 0.10.
* **Xero EOD invoices**: `LineAmountTypes: 'Inclusive'`, `TaxType: 'OUTPUT'` — Xero receives the tax-inc amount and extracts GST itself.

### POS PIN Login
* Replaced email+password login with staff-picker + PIN UI.
* `pos_pin_hash VARCHAR(255)` column added to `users` table (migration: `scripts/add-pos-pin.mjs`).
* New APIs: `GET /api/pos/auth/staff?location_id=X` (returns staff list with `has_pos_pin`), `POST /api/pos/auth/pin-login` (bcrypt PIN verify).
* IMS Users edit modal supports setting/clearing POS PIN.
* Admin email+password login still available as fallback link.

### Register Lifecycle
* `pos_registers` and `pos_register_sessions` tables manage register state.
* Opening a register blocks re-opening (409 conflict). Register must be closed at EOD.
* `GET /api/pos/register/session?register_id=X` — returns current open session if any.
* `RegisterGate` screen shown when a stale open session is detected on login.

### EOD Reconciliation
* Cash Counted ($) is now a direct input field (not just via denomination count).
* Denomination count auto-syncs the Counted field when updated — both methods work.
* `updateEntry` for `denominations` key also sets `counted = String(calcCash(newDenoms))`.
* All payment methods use `parseFloat(e.counted)` for the counted value (no more `calcCash` in saveEod).

### SOH Sync
* Instant in-memory SOH patch on sale complete (decrements sold qty, increments on return).
* Background sync every 5 min via `setInterval` in `useEffect` when screen is `'pos'` or `'receipt'`.
* **Bug fixed (2026-06-23):** Background sync `useEffect` was placed after early returns — violated Rules of Hooks → React error #310. Fixed by moving it before all early returns in `PosPage`.

### POS Sales View (IMS)
* Expanded sale dropdown now shows: `#txnId` badge, payment split strip, line item columns Ex-Tax | GST | Total (inc).
* `/api/ims/pos-sales/day` now fetches payments per sale and attaches `payments[]` to each sale object.

### Service Worker
* Cache version `v2` — bumped to evict stale cached pages.
* `res.clone()` race fixed: clone immediately before returning response, then put clone in cache.

### Deployment
* **Hosting moved to Railway** (not Vercel). Auto-deploys from `main` branch.
* Two MySQL DBs on Railway: main (`MYSQL_DATABASE` — users, business config) and IMS (`IMS_MYSQL_DATABASE` — all POS/IMS tables).
* Auth cookies: `marketoir_session` (admin/IMS), `pos_session` (POS cashier, 16-hr).

### Multi-tenant IMS provisioning (2026-07-21)
* New homepage registrations must provision a dedicated IMS schema and set `businesses.ims_db_name`; otherwise the business falls back to the default Monsterthreads IMS schema.
* Public `/api/auth/register` now calls `provisionBusinessIms()` after creating the business row.
* `scripts/ims-schema.sql` is the source for fresh tenant schemas. Keep it aligned with live migrations, including `business_id` columns, POS register/session columns, `ims_sales_history`, and `trg_ims_stock_bizid` / `trg_ims_sales_cache_bizid` compatibility.
* IMS API routes that touch IMS tables must call `getImsSession()` or `enterImsForBusiness()` before any `imsQuery`, `imsExecute`, `getIMSPool`, or `Ims*Repo` call. Local cookie parsing alone is not tenant-safe.
* Sage was provisioned as `readyedu_SageIMS`; leak-prone tables verified empty after provisioning.

### Tenant gatekeeper rewrite (2026-07-21) — ROOT CAUSE of cross-tenant leaks
* `AsyncLocalStorage.enterWith()` inside an awaited function does NOT propagate back to the caller, so `await enterImsForBusiness()` / `getImsSession()` **never actually bound the tenant schema** — every IMS query silently fell back to `IMS_MYSQL_DATABASE` (= Monsterthreads). This is why Sage saw Monsterthreads data everywhere despite per-route fixes.
* Fix: `imsQuery`/`imsExecute`/`getIMSPool` are now strict tenant gatekeepers. Per-call resolution: explicit db arg → bound context (`runImsForBusiness`) → session cookie (`marketoir_session`/`pos_session`/`wholesale_session` → `businesses.ims_db_name`) → env default only when NO session exists. A signed-in session with no schema mapping **throws** (fail closed) instead of falling back.
* No-cookie flows must use `runImsForBusiness(businessId, fn)` (callback form — the only ALS pattern that propagates). Converted: online-sales auto-sync-cron, shopify payout-sync cron (now discovers businesses from the main registry, not one tenant schema), shopify sync-inventory cron drain (per-tenant), shopify orders webhook, POS pin-login (explicit db arg), POS chat SSE stream (explicit db into detached timers).
* `enterImsForBusiness()` is deprecated (harmless cache warmer). `getImsDbNameStrict()` returns the mapping or `undefined` — never the env fallback, and never caches the fallback.
* Dashboard onboarding is business-scoped via `/api/onboarding`; it stores progress in tenant `ims_settings.onboarding_completed_steps` and reuses existing IMS settings keys for business profile, operations, tax, Shopify, and Xero setup prompts.
* Stocktakes and branch transfers were originally created by standalone migrations without `business_id` and were missing from `scripts/ims-schema.sql`; keep both table families tenant-scoped in repo/routes and fresh schema provisioning.

---

## 📦 IMS — Purchase Orders (2026-06-24)

### Partial Receives & Backorder POs
* `POStatus` type now includes `'partially_received'` (between `approved` and `received`).
* Smart device `/receive` page has two submit buttons: **Save Progress** (partial, stays on page) and **Mark as Received** (finalise).
* **Save Progress** sets PO to `partially_received`; items accumulate across multiple receive sessions.
* **Mark as Received** with shortfall prompts user to create a **Backorder PO** for missing items.
* Backorder POs named `{original}-B` (e.g. `PO-2025-0042-B`), falling back to `-B2`, `-B3`; start in `draft`.
* From the IMS PO list, `partially_received` POs show: "Continue" (device link), "Mark Received", "Revert to Approved" (view context), "Cancel".
* **Revert to Approved** from `partially_received`: fully reverses all stock updates (decrements `qty_on_hand`, restores `qty_incoming`, resets `qty_received = 0` per item).
* **Cancel** from `partially_received`: reverses on-hand stock AND remaining incoming, then cancels.

### Batch Receive API (`/api/ims/receive/batch`) — Bug fixes + new fields
* **`qty_received` now accumulates** (`qty_received = qty_received + ?`) — multiple partial sessions are safe.
* **`qty_incoming` is now decremented** on each item received (was missing before).
* Returns `shortfallItems[]`, `newStatus`, `allReceived`, `backorderPoId`, `backorderPoNumber`.
* Fires `triggerPOXeroSync(businessId, poId, 'received')` post-commit when PO is fully received.
* Fires `refreshVariantCache` post-commit for all received variant IDs.

### Xero on Revert/Cancel (2026-06-24)
* **PO revert/cancel** → attempts to void the Xero Draft Bill (always safe — no payments on drafts).
* **SO revert/cancel** → voids Xero Invoice only if `AmountPaid === 0`; if payments exist, returns a warning requiring manual Xero reconciliation.
* Both operations are non-blocking; failures logged to `xero_sync_log` and surfaced as `xeroWarning` in the API response / UI alert.
* PO route Xero dispatch is now explicit: `cancelled` → void, `draft` → void, `received` → approve bill, `approved`/`partially_received` → no Xero action (prevents duplicate draft bill on revert).

### `changeStatus` transitions (ImsRepository.ts)
* `partially_received → received`: processes only remaining items (qty_ordered − qty_received) with full landed cost + avg cost calculation.
* `partially_received → approved`: reverses received stock, restores incoming, resets qty_received = 0.
* `partially_received → cancelled`: reverses received stock + remaining incoming qty, resets qty_received = 0.
* `received → ordered`: reverses all received stock (qty_on_hand −= qty_received, qty_incoming += qty_received), inserts `po_unapproved` movements, resets qty_received = 0, clears received_date. Used for PO revert from received state.

### PO Xero Sync Flow (as-built 2026-06-24)
| Status | Xero Bill | Edit behaviour |
|--------|-----------|----------------|
| draft | — | No Xero |
| ordered | DRAFT Bill | Auto-syncs DRAFT on edit (triggerPOXeroUpdate) |
| partially_received | DRAFT Bill | Auto-syncs DRAFT on edit |
| received | AUTHORISED | ⚠️ xeroWarnModal shown for edit/delete (bookkeeper message) |
| cancelled | VOIDED | Edit/Delete allowed (no Xero) |

* **PO Due Date in Xero**: `supplier_invoice_date + payment_terms_days`; falls back to `order_date + payment_terms_days`; then `order_date`. Set via `calcDueDate()` in XeroSyncService.ts.
* **PO received edit/delete**: shows warning modal with draft bookkeeper message; Xero NOT auto-synced (AUTHORISED bills can't be updated via API).
* `triggerPOXeroUpdate` — updates DRAFT bill on PO edit; silently skips if AUTHORISED.
* `triggerPOXeroVoid` — voids bill on revert/cancel; for `received → ordered` revert: voids AUTHORISED bill then creates new DRAFT.

### SO Xero Sync Flow (as-built 2026-06-24)
| Status | Xero Invoice | Edit behaviour |
|--------|-------------|----------------|
| draft | — | No Xero |
| confirmed | DRAFT Invoice | Auto-syncs DRAFT on edit (triggerSOXeroUpdate) |
| fulfilled | AUTHORISED | ⚠️ xeroWarnModal shown for edit/delete (bookkeeper message) |
| cancelled | VOIDED (if no payments) | Warning if payments exist |

* `triggerSOXeroSync('confirmed')` — creates DRAFT ACCREC Invoice (NOT AUTHORISED — changed from original).
* `triggerSOXeroSync('fulfilled')` — approves the invoice to AUTHORISED; awaited in route (ensures approval before response).
* `triggerSOXeroUpdate` — updates DRAFT invoice on SO edit; silently skips if AUTHORISED.
* `approveInvoice()` in XeroSyncService.ts — mirrors `approveBill` for ACCREC type; logs as `so_invoice`.
* `updateXeroDraftInvoice()` in XeroSyncService.ts — mirrors `updateXeroDraftBill` for ACCREC; checks DRAFT status first.
* **Backward compat**: Old confirmed SOs with AUTHORISED invoices — `updateXeroDraftInvoice` skips; `approveInvoice` on already-AUTHORISED is a no-op in Xero.
* **GET /api/ims/xero/invoice-details?soId=X** — returns `{invoiceNumber, total, subTotal, taxTotal, status}` from Xero; mirrors bill-details endpoint.
* `SoAccountingSection` shows live invoice # + Xero total; mismatch warning if IMS ≠ Xero total.
* SO fulfilled edit/delete: `editSoWithWarn`/`deleteSoWithWarn` helpers (same pattern as PO's `editPoWithWarn`/`deletePoWithWarn`).

### Modal z-index fix (2026-06-24)
* Warning modals (xeroWarnModal, soXeroWarnModal) must render when viewModal is CLOSED, not open.
* Fix: `beforeAction?.()` (which closes viewModal) is called BEFORE `showXeroWarnForReceived` / `showSoXeroWarnForFulfilled`, not inside the onConfirm callback.
* This ensures the warning modal renders on top of everything.

### Shopify retail customer sync (2026-07-23)
* Added two-way retail customer sync foundation under `src/app/api/ims/shopify/sync-customers/route.ts` with modes:
  * `pull` (Shopify -> IMS): imports all Shopify customers, matches by `shopify_customer_id` first, then email fallback, fills blank IMS fields only.
  * `push` (IMS -> Shopify): syncs `retail_customer` contacts to Shopify on demand.
* Added contact linkage field `ims_contacts.shopify_customer_id` with unique index `(business_id, shopify_customer_id)`.
  * Bootstrap schema updated in `scripts/ims-schema.sql`.
  * Tenant catch-up updated in `scripts/catchup-schema-all-tenants.mjs`.
  * Runtime guard migration helper added: `src/lib/ims/ensureContactShopifyCustomerSchema.ts`.
* Added outbound sync helper `src/lib/ims/shopifyCustomerSync.ts` and wired contact save hooks:
  * POST `src/app/api/ims/contacts/route.ts`
  * PUT `src/app/api/ims/contacts/[id]/route.ts`
  * Contact saves remain non-blocking; API returns `shopifySync` result/warning.
* Added Shopify customer operations in `src/services/ShopifyService.ts`:
  * `getAllCustomers`, `findCustomerByEmail`, `createCustomer`, `updateCustomer`, `disableCustomer`.
* Soft-delete mirroring (outbound): when IMS retail contact is set inactive and has linked Shopify id, sync attempts to disable Shopify customer.
* IMS Shopify UI (`src/app/ims/components/ShopifyView.tsx`) now includes:
  * Pull button, Push button, run summary, and unresolved gift-card customer link examples.
* Gift-card linkage visibility:
  * Sync response now includes `matchedGiftCardCustomers`, `missingGiftCardCustomers`, and `missingGiftCardExamples` sample rows.
* Required Shopify scopes for this flow:
  * `read_customers`, `write_customers` (plus existing product/order/inventory scopes).

### IMS reports extraction hardening (2026-07-23)
* `src/app/ims/page.tsx` had prior structural corruption after a large patch attempt in the report section. Safe recovery path was: restore from `HEAD`, then re-apply in small bounded edits.
* Extracted/wired report views now use wrappers in `src/app/ims/page.tsx`:
  * `SalesByBranchView` -> `src/app/ims/views/reports/SalesByBranchView.tsx`
  * `SalesSearchView` -> `src/app/ims/views/reports/SalesSearchView.tsx`
* Shared report filter/date helpers moved to `src/app/ims/views/reports/reportFilterHelpers.tsx` and imported back into `page.tsx` (behavior preserved).
* Practical rule for this monolith: avoid single giant diffs; patch by anchor in small chunks and run diagnostics (`get_errors`) after each chunk.

### PO/SO amendment history detail (2026-08-11)
* PO and SO order modals now summarize amendment lines by added/removed/updated count and provide an expandable `View changes` section.
* Expanded detail shows safe human-readable changed header fields, variant IDs, and quantity transitions; raw audit JSON and internal header fields are not exposed.
* Example: changing 5 red to 3 red plus 2 blue displays one updated line (`5 to 3`) and one added line (`2`).
* Both operation and line-history reads remain explicitly tenant-scoped by `business_id`; the enriched SQL was exercised against Monsterthreads and Sage.
* Validation: focused history/PO/SO route tests passed (26), full Vitest passed (263 files / 1,253 tests, one skipped), and the production build passed.
* Three lifecycle-hardening slices remain: receive-batch request replay safety; unified receive/fulfil/shortfall activity history; final browser/end-to-end exercise and dead-path cleanup.

### PO receive replay + unified order activity (2026-08-11)
* `POST /api/ims/receive/batch` now binds each request operation key to a SHA-256 request hash in `ims_po_receive_operations`; completed retries return the stored response before PO status or stock checks and do not repeat stock movements.
* Desktop and scanner receive clients derive stable keys from the loaded PO revision plus exact payload. The desktop preliminary PO edit also uses a stable revision/payload key so a lost receive response can reach both edit and receive replay.
* Example: receiving red quantity 2 twice with the same key leaves SOH +2 with one `po_received` movement; reusing that key with quantity 3 returns HTTP 409.
* PO/SO Activity History now combines amendments with receive/fulfilment and shortfall-resolution ledgers, sorted newest first. Safe request snapshots show received/shipped quantities; raw JSON remains server-side.
* Added `request_json` to PO receive and SO fulfilment ledgers through the all-tenant catch-up migration. Verified PO/SO activity union queries in Monsterthreads and Sage.
* One lifecycle-hardening slice remains: final browser/end-to-end operational exercise and obsolete/dead-path cleanup.

### PO/SO lifecycle hardening sign-off (2026-08-11)
* Final staff call-graph review confirms PO physical completion is owned by batch receive and SO physical completion by the fulfil endpoint. Direct SO `fulfilled` repository calls remain only for Shopify import/webhook compatibility.
* Removed stale generic PO `complete` / `partially_received` confirmation labels and renamed the modal component to `OrderActivityHistory`; the response field remains `amendment_history` for API compatibility.
* Post-commit PO receipt Xero approval failures, including replay recovery attempts, now call `reportRuntimeIssue()` with tenant/order context while receipt replay still returns its committed success.
* Live schema audit passed in Monsterthreads and Sage: receive operation hash/key/snapshot columns, unique `(business_id, operation_key)`, and order lookup indexes are present. No production lifecycle operation rows existed, so no synthetic business records were persisted.
* Browser smoke reached the authentication boundary and redirected `/ims` to `/login` cleanly. Protected modal interaction could not be automated without a shared authenticated tab or credentials; route/service tests cover the protected workflows without exposing secrets.
* Lifecycle hardening is complete. Future work is ordinary regression maintenance rather than a planned follow-on slice.

### CN/SCN Xero pathway hardening (2026-07-25)
* `POST /api/ims/xero/void` now supports `type: 'cn' | 'scn'` in addition to PO/SO.
* Added service-level Xero void functions for credit notes:
  * `voidXeroCreditNote()` for customer credit notes (ACCREC)
  * `voidXeroSupplierCreditNote()` for supplier credit notes (ACCPAY)
* Added hook-level orchestration:
  * `triggerCNXeroVoid()`
  * `triggerSupplierCNXeroVoid()`
* Complete endpoints for CN/SCN remain async fire-and-forget, but now return explicit queue metadata (`xeroSync.state/queuedAt/retryEligible/pollEndpoint`).
* Added CN status polling endpoint parity at `GET /api/ims/credit-notes/[id]/xero-status`.
* Unified Xero sync history (`/api/xero/sync-log`) now includes CN/SCN events (including void events), not just PO/SO/EOD/COGS.
* Xero Sync tab now recognizes CN/SCN (+ void) event types with labels/colors, link routing, and retry actions for failed CN/SCN sync attempts.
* CN and SCN view modals now include a `Void in Xero` action when synced.
* `POST /api/ims/xero/push` now guards CN/SCN retries with MySQL named locks (`GET_LOCK`) to prevent duplicate concurrent retries, and short-circuits with `skipped: true, reason: 'already_synced'` when a credit note already has synced status + stored Xero ID.
* Tenant migration safety fix: `scripts/catchup-schema-all-tenants.mjs` now bootstraps CN/SCN tables when missing, catches up CN/SCN columns/indexes, and extends enums for `ims_credit_notes.status` (`awaiting_product`) and `ims_stock_movements` (`cn_returned`/`scn_returned`, `credit_note`/`supplier_credit_note`).
* Baseline provisioning fix: `scripts/ims-schema.sql` now includes CN/SCN tables (`ims_credit_notes`, `ims_credit_note_items`, `ims_supplier_credit_notes`, `ims_supplier_credit_note_items`, `ims_supplier_credit_note_files`) and the required stock movement enum values so new tenants don't miss credit-note structures.

### IMS Location Daybooks and POS capability (2026-08-27)
* Added **Locations > Location Daybooks** so IMS managers can select an active location and use its full Store Daybook with the signed-in IMS account retained in the audit trail.
* Added the tenant setting `business_requires_pos` to onboarding Operations and IMS General Settings. Existing businesses default to `yes`; Location Daybooks also remains available when an active location is marked for POS, has a POS enrolment code, or owns an active register.
* IMS-selected Daybook locations authenticate only through `marketoir_session` and are revalidated against the current tenant. POS sessions remain pinned to their enrolled location and cannot submit a different location override.
* Corrected phase-selector CSS so badge colours apply only to OPEN/TODAY/CLOSE badges; titles and completion counts retain readable neutral text.
